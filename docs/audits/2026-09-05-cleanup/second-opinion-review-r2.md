# Fable 5.1 High follow-up review — September 6

The user requested a fresh critical review after more than twenty-four hours of work and selected Fable 5.1 High in Claude. Both settings were visibly confirmed. The exchange took place in the open DevRyan review chat through computer use. Fable received the approved outcomes, delivered fixes, measured results and limitations, repeated QA failures, and the latest `HaFl6X` Plan-off failure. It inspected relevant local source under explicit read-only instructions and answered a corrective follow-up. This is advisory review, not additional execution evidence.

## Decisions adopted

- Keep Plan authority in existing canonical history and recorded message state. Do not add another durable session policy or a persistent memory system for this defect.
- Separate Plan restoration from the model/effort latest-user selector. Skip narrowly recognized maintenance turns, preserve ordinary synthetic-user compatibility, and give explicit Implement Plan precedence over stale Plan flags.
- Reproduce the newest-user/missing-parts case as part of the same small fix. A restore must wait for the relevant parts rather than permanently adopting an older non-Plan value. Existing explicit user choices must still win.
- Preserve the existing full Plan instruction in automatic continuations when the current authoritative turn requires it. Verify the resulting managed-task read-only policy and approval transition. Do not describe a prefix-only marker as equivalent to the full instruction.
- Keep automatic maintenance responses out of fresh Plan-card and plan-ready presentation. Adding a Plan instruction to a wake must not silently create another actionable plan.
- Primary-agent Plan mode currently relies on prompt instructions; managed children receive separate enforced read-only restrictions. Restoring continuation behavior does not create new primary-agent write enforcement, and the failed run did not demonstrate unauthorized writes.
- Build/package the corrected source before its live journeys. Run meaningful focused regressions during the fix and one required full gate on the settled source. Reuse earlier evidence only through its actual UI/backend/runtime dependencies.
- Replace run-count-driven execution with a short coverage map of missing outcomes. The 96/76 expansions are implementations of coverage, not goals in themselves. Keep both native compaction paths and all promised role/runtime/control behavior explicit.
- Preserve the measured CPU/GPU results and missed historical percentage targets. Do not make speculative changes to chase percentages that lack a matching before/after implementation difference.

## Advice corrected or not adopted

Fable initially proposed skipping all synthetic non-Plan messages. The coordinator pointed to the existing compatibility regression; Fable withdrew the blanket rule and recommended exact maintenance recognition, including a narrow legacy match for the existing open-todo prompt. The general model/effort restoration contract stays unchanged.

Fable initially recommended copying Plan instructions without accounting for presentation. It accepted the need to distinguish continuation policy from a fresh plan response. It also agreed that arbitrary message metadata must not be assumed durable without native evidence.

Fable described two boundaries as never observed. Earlier manual preflights did observe two summaries; what remains missing is complete two-boundary operational continuity through implementation and application verification. Its follow-up corrected that claim.

The coordinator rejected dropping typing and lifecycle-memory observations merely because the harness had failed, and rejected a minimum list that omitted Builder/web/mobile coverage without an applicability map. Fable restored those outcomes. Its suggested direct input and fixed-point memory measurements are candidates for a smaller prospective protocol, not completed measurements. Basic workload validity, artifact comparability, and required journal diagnosis after an actual runtime failure remain necessary.

The coordinator also does not adopt a blanket claim that two journeys per provider cover every behavior. Coverage must be mapped to actual observations. xAI's unavailable access remains an explicit provider blocker; independent available-provider work can proceed.

The literal approved plan was subsequently checked in `.cache/audit/coding-agents-2026-09-05/approved-plan.md`. It explicitly requires every selected model × primary role × Plan flag × web/Electron cell, both thinking states in each, and natural Electron compaction for every selected model × primary role × Plan flag. It also explicitly requires per-run artifact, selection, correlation, visual, journal and cleanup records and three fresh observations per side for performance comparisons. Fable's shorter proposed journey list does not satisfy those requirements by itself. Simplification must come from combining compatible observations, removing repeated review overhead and justified evidence reuse—not silently dropping required cells or records. The later user clarification changes the model set to one main model per provider, with focused Spark coverage.

## Bounded finish rules

1. Every new test or diagnostic must answer a named unresolved product question. No broader harness work merely to improve the evidence machinery.
2. Fix the latest Plan failure, including approval and partial hydration, before another expensive operational journey. Use deterministic regressions before native execution.
3. A QA-only failure gets one focused correction and a new, separately identified attempt. A second QA-only failure on that same objective stops harness expansion; use a simpler direct native route with the required observations, or record the item as unverified. Never promote a failed run to passing coverage.
4. Finish manual and natural two-boundary continuity, actual implementation, and managed-result reconciliation on available providers. Retain Builder, web/mobile and Default/explicit effort observations through applicable evidence plus fresh affected checks.
5. Obtain the missing typing and lifecycle-memory observations with a small matched protocol. Keep existing resource/backend results; do not reopen unrelated historical failures.
6. Final acceptance still requires the promised outcomes and final validation. Report unavailable provider coverage or unsuccessful required checks precisely rather than extending unrelated work indefinitely.

The earlier [review](second-opinion-review.md), failed cohorts and all measured limitations remain preserved. The latest operational failure and current source state are recorded in [the continuation plan](resume-plan.md).

The user then requested a clean stopping point before `/compact` and a switch to Plan mode to review the finish plan. The UI/backend correction is checkpointed with 301 focused tests, UI type-check and changed-file lint passing. The performance-helper proposal stopped before implementation. Further builds, full validation and live/performance runs are held for that planning turn. [The handoff](finish-plan-handoff.md) records exact state and limits.
