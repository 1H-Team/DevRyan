# Coding Agents packaged-instruction review

F09 retains the shipped workflow contracts. Three fresh matched compacted/control pairs did not reproduce unresolved missing-plan inference and do not justify a continuity-instruction change for that symptom.

The packaging authority is `packages/web/server/default-config`, inventoried by `lib/opencode/default-config-assets.js`. Its managed roots are `agents`, `plugins` and `user-profile`, plus root `opencode.json`. The existing packaged-config smoke checks the provisioned agent files and exact managed manifest. Repository-local `.opencode` configuration is not a distributable input to this inventory. Its machine-specific permission paths are left unchanged under the requirement to preserve personal configuration.

The canonical Builder prompt is 7,479 bytes and Orchestrator is 34,572 bytes at this checkpoint. The inventory of ten files under canonical `agents/` contains no `/Users/`, `/home/` or `/root/` paths. Exact sizes and SHA-256 values are retained in `.cache/audit/coding-agents-2026-09-05/packaged-instruction-review.json`. This targeted path check is not a claim that arbitrary absolute paths are invalid: runtime-provided exact paths are necessary inputs to the tool contracts.

Builder's direct execution, plan-card approval, current-plan todos, bounded tool recovery and leaf-only permissions remain necessary. Orchestrator's ownership, three-child waves, bounded assignments, result disposition, task recovery and final review closeout govern different transitions; their size alone does not establish redundancy. Existing native `task` restrictions, managed-task identities, plan write gates and runtime provisioning checks remain in place. The separately shipped legacy Tauri assets are intentionally preserved for released compatibility; this audit does not reconcile them with Electron by copying current prompts into Tauri.

One xAI natural-compaction preflight retained the original objective, latest plan revision, exact plan path and approval boundary in its native summary. Afterward, the model used two globs that omitted hidden files and reported the plan missing without directly reading the retained path. The file still existed unchanged, and the journal had no gaps. Inspection of the pinned native glob implementation confirmed that those calls did not enable hidden-file discovery. This establishes a retrieval/inference failure in that run, not omitted summary context. The current prompts already require grounded exact paths and narrow rediscovery, but do not explicitly warn against treating a non-hidden glob miss as proof that a known hidden plan is absent.

The six fresh control/compacted diagnostic arms are now classified with all outcomes retained. Every compacted arm read the exact saved plan successfully; all six preserved revision 2 and the implementation pause, and did not repeat completed source reads or failed verification during the diagnostic pause. Five arms passed. The final compacted arm remains failed because it raised an unanswered native approval question after reading the plan, while its control settled with text. Another compacted arm retained one rejected PNG-as-text read without retry. These are recorded separately from missing-plan inference; neither establishes summary loss. The study has 4,862 reviewed journal records with no gaps and 41 reviewed run screenshots. Exact evidence and the separately corrected cleanup limitation are in `coding-agents-acceptance.md` and `.cache/qa/diagnostic-VoC1qS/retrieval-xai/study-review.json`.

No persistent checkpoint or competing task ledger has been added. This manual-summary retrieval diagnostic does not establish general retention, natural threshold-driven compaction, or automatic continuation. Any remaining operational loss must be demonstrated by those separate checks before selecting a record, retrieval, native-hook, or checkpoint correction.
