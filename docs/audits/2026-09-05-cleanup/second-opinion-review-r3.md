# Finish-plan review with Fable 5.1 High — September 6

After approving the finish plan, the user requested another back-and-forth in the existing Claude chat, **DevRyan coding-agent reliability review**. Fable 5.1 and High effort were visibly selected. The coordinator submitted the approved plan with the literal original coverage requirements, received a source-informed critique, challenged unsupported claims and proposed substitutions, and read the revised response. This was a read-only advisory exchange; it supplies no new runtime acceptance evidence.

## Amendments adopted within the approved scope

- Before natural journeys, test canonical Plan authority beyond the current 100-record read: the latest authoritative human just outside the window, a maintenance parent exactly at its edge, and an ordinary synthetic non-Plan user as the nearest earlier authority. Preserve exact causal parent anchoring. The installed v2 SDK exposes `before` and `limit`, but its types do not establish response ordering or the injected plugin client's contract. Verify those contracts before implementing bounded backward retrieval if the edge is reproduced. Missing authority must remain an explicit failure; neither UI pending-plan state nor arbitrary later approval is backend authority.
- Existing packaged-config smoke verifies assets, registrations, staging and dependencies; inspection does not establish that it launches OpenCode. On the fresh package, verify actual managed-plugin registration through the existing runtime evidence path, such as the served tool inventory or a definitive startup record. Reuse an existing assertion if present rather than add another smoke framework.
- Run the quiet typing and lifecycle-memory comparisons after packaging and prerequisite checks, before provider journeys can contaminate host measurements. Manual and natural representatives count toward the cells they actually exercise. Default and explicit effort can occupy distinct human turns in one compatible journey, with separate requested/effective evidence.
- Pagination requires the actual click's newly returned first message ID and canonical text visibly committed in the selected session, plus fresh contiguous cursor coverage. Do not require every loaded row to be mounted. Any normal reveal scrolling occurs before the settled memory sampling window. Store membership, HTTP completion and virtualizer counts alone are insufficient.
- Apply the retry bound across repeated occurrences of the same failure class in a harness component, so renaming a scenario cannot restart the correction cycle. Unrelated defects are not automatically the same class. After the repeated QA-only failure, use the direct route with required observations or record the outcome unverified.
- Record input-token changes as compaction corroboration, not a universal pass condition. Canonical boundaries, native lifecycle and behavioral continuity remain mandatory; refilled tool context and adapter accounting can prevent a lower next-turn input count.

## Advice corrected or rejected

Fable explicitly withdrew its statements that two plugins never loaded and nine of fifteen sessions were blocked by usage limits. It said these came from other sessions, not this task. They must not enter the findings register. It also withdrew a 33-file framing: the correction checkpoint covers seventeen files, while the larger working tree includes earlier and separately owned changes.

After inspecting `selection-store.ts`, Fable confirmed inferred-state refinement and explicit same-value toggle protection are already implemented. These remain integration-verification obligations, not newly discovered missing code.

Fable withdrew the store/virtualizer-only pagination oracle and mandatory lower-next-input-token gate. Fixture mobile coverage does not replace required real web live cells. Existing authorization already permits compatible effort observations in one journey and explicit blocked-provider reporting; neither requires a new approval question. Natural provider concurrency depends on demonstrated isolation, resources and admission, not the withdrawn load claim.

The 24 live combinations with both effort states, twelve natural Electron combinations, two successive boundaries, real implementation and verification, responsive coverage, per-run records and three observations per performance side remain unchanged. No new memory system, new dependency, threshold reduction or permission architecture was authorized by this review.

## Current qualification

The source checkpoint and 301 focused passing tests remain those in [the handoff](finish-plan-handoff.md). No build, full gate, provider run or benchmark was performed during this exchange. The next work is integration verification and the bounded measurement-helper changes, then qualification of the fresh candidate.
