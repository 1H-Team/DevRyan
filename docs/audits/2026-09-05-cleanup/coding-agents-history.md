# Historical Coding Agents evidence

This preserves earlier checkpoints and failed attempts. Present-tense status and launch instructions below describe their original time only. [Current acceptance](coding-agents-acceptance.md) supersedes them.

# Coding Agents acceptance — implementation in progress

This follow-up covers Builder and Orchestrator in Electron, web, and responsive web. Bots, VS Code runtime QA, personal configuration changes, other repositories, and release publication are excluded. Existing shared-contract compatibility tests remain required.

Latest source checkpoint for the user's `/compact` and Plan-mode discussion: source `703b2c72…` contains the bounded Plan restoration, maintenance-presentation and backend continuation corrections. All 301 focused tests pass, alongside UI type-check, changed-file lint and root whitespace checks. The existing package and full gate below predate this correction. No new package, global gate, live journey or performance run is claimed. Workers are stopped; the performance helpers remain proposals. [The handoff](finish-plan-handoff.md) is the next-turn authority for work status and remaining decisions.

Latest operational result (September 6, 02:03 UTC): `planref-r3` / `HaFl6X` is closed failed. Two saved Plan revisions and the first genuine native manual compaction passed; after UI reload, the next actual human steering was submitted with Plan off instead of on. All five run screenshots are reviewed. Independent closure verifies the retained plan and fourteen original fixture files, 2,618 parseable gap-free journal records, and absence of all 92 recorded process identities. There is no second-boundary or implementation acceptance and no demonstrated unauthorized mutation or persistent-memory loss. The cold restoration source and backend synthetic-continuation policy require a product correction; the earlier package/full-gate results below describe the unchanged pre-correction source. The user's requested Fable 5.1 High review is assessing that fix and a smaller outcome-based finish plan before further live execution.

Current state (2026-09-06, after current measurements): full live acceptance remains open. The current package is `packaged-electron-mUsL9F`, with staged UI `candidate-web-OxzBwy`; the fresh baseline package `packaged-electron-4TOqUt` has the same current backend/native bytes and the original baseline UI. UI/web builds, native packaging and the fresh complete lint/type/test gate pass with no source/scripts drift. Web startup is 1,363,364 gzip / 4,653,888 raw bytes, within unchanged budgets; the excluded VS Code bundle still exceeds its limits. Qualification and exact identities are retained in `.cache/audit/coding-agents-2026-09-05/post-profile-corrections/`.

The focused Spark Explorer failure is reproduced and corrected: Spark rejects the summary option, so its exact model targets now retain High without requesting a summary. The corrected Spark/Sol pair passes independent task, result, journal and cleanup review. Delayed historical parts also no longer overwrite newer unsent effort choices; eight mounted regressions and full validation pass. These results do not replace final UI journeys.

The earlier 96-scenario proposal was activated on `Q1gHWd`. Its OpenAI Electron Orchestrator + Plan High project journey `qDE88v` passed nine checks through the revised paused plan and mixed task readiness, then was stopped after the first native compaction remained unobserved. The evidence cannot distinguish a missed click, submission lock or pending summarize transport. That failed original and its 95 unrun scenarios remain preserved. The corrected keyboard submission helper now passes a fresh current Electron core fixture: both queue settings crossed with both activity transitions each produce exactly one summarize request, without a scoped abort or ordinary prompt. Its 404 responses prove routing only. Independent review covers 1,618 journal records with no gaps or parse errors, all 37 screenshots, all 23 recorded process identities absent and private runtime removal. Fresh manual and natural representatives still need two native boundaries and separately graded implementation/task reconciliation before broad execution. Current xAI access remains unavailable; each provider run requires fresh credential admission. The fresh `2aFUr9` manual representative subsequently stopped after both saved Plan revisions passed preservation checks, before either native boundary: the helper required a separate project plan file that Plan-mode instructions told the agent not to write. All three screenshots are reviewed; the application saved and displayed both revisions. The failed run stays preserved while the QA plan-reference contract is corrected.

Fresh current resource comparisons have three observations per side: one-stream CPU changes +0.63%, within its +5% limit; four-stream CPU changes +1.64%, missing the −25% target; renderer/total working sets fall 3.01%/5.96%, both within +5%. GPU changes −1.62%, missing the −50% target. The total-memory ranges separate; CPU/GPU ranges overlap. Independent applicability review confirms both packages already contain the same skeleton animation and streaming throttle. All eighteen resource arms pass technical review. Typing and session-memory stop at their first prerequisite failures, leaving five arms unrun each. The original current backend block also stops on an invalid CPU-profile timestamp assumption. After a peer-reviewed normalization correction and a fresh full gate, a separately declared six-arm current-graph backend study passes: median cold discovery falls 12.19% and median per-arm maximum health delay falls from about 3,386 to 3 ms. All 53 recorded backend process identities are absent. Original failures and older graph results remain preserved; no historical observations are pooled. A single-click diagnostic selects the requested session successfully, but does not explain the original typing failure or provide typing measurements. See [measurements](performance.md) for scope and evidence. No persistent memory system has been added; historical results below retain their original qualifications.

The later Plan-reference correction tracks the original saved session revision inside the prepared runtime's fixed plans directory and checks its identity, path and bytes at every paused boundary. Approval requires an observed native read and a fresh complete canonical Plan card. Both roles also receive a strict native source-read/failing-test/source-change/passing-test gate, including actual managed child scopes. Seventy-four focused tests, independent reviews and combined full validation pass with no source/scripts drift. The fresh `planref-r2` manual journey ended at a QA ReferenceError after the first completed Plan turn: its manual adapter omitted the `findQaSubmittedUser` import. Both screenshots are reviewed, but no revision capture or native boundary completed. The import correction now has an actual-path regression that reproduces the error before the fix. The single-import correction and two actual-adapter regressions now pass all twenty scenario tests, a targeted undefined-binding scan and independent review. Cleanup confirms all 67 process identities absent and all fourteen original/archive files unchanged; 1,711 gap-free journal records include 21 cleanup records beyond the copied manifest totals. Root released the fresh `planref-r3` manual run under this scoped qualification, with unchanged application/package bytes and all original two-boundary requirements. Natural and broad execution remain held; final full validation still precedes complete acceptance. These QA-only changes leave application/package bytes unchanged and do not turn historical failures into passing coverage. Exact changes, preserved failures and applicability are recorded in `manual-plan-and-causal-corrections-r1/` and `manual-plan-reference-correction-r{1,2}/` in the audit cache.

## Frozen live model selection

The authenticated host catalog was inspected on 2026-09-05. Configured Coding Agents select OpenAI `gpt-5.6-sol` and xAI `grok-4.6`. Anthropic has no assigned Coding Agent, so its available reasoning default `claude-sonnet-4-6` completes the selected set. All three report connected access. This is discovery evidence, not successful live acceptance. Specialist assignments outside these providers will be replaced only in the isolated QA profile and recorded.

## Baseline and evidence

- Revision: `ff7abd116ca37db53a56981d7de76100f2a97690`, with the existing dirty worktree preserved in `.cache/audit/coding-agents-2026-09-05/baseline.patch` and `baseline-untracked.tar` before this follow-up's edits.
- Build manifests and runtime metadata: `baseline.json` in that evidence directory.
- Existing bundle gate: web 5,048,720 raw / 1,481,202 gzip bytes, exceeding unchanged 4,962,877 / 1,456,388 limits. VS Code also reports an existing overage; runtime optimization there is excluded.
- The initial full-validation attempt overlapped newly added regression tests. Its two empty-thinking failures were the new tests running before the implementation landed. It is development RED evidence, not a clean pre-change validation result. The previous pass's final full validation is recorded in the main report.
- Native provider-default probe: actual OpenCode 1.18.27 and pinned 1.18.29 both resolve an omitted variant to the configured agent's `medium`, an explicit empty variant to no provider effort override, and `high` to `high`. The probe uses a local model endpoint; it is not live-provider acceptance.
- OpenCode 1.18.29 was downloaded only into ignored QA storage and verified against its npm SHA-512 integrity. The user's installed runtime was not replaced.

## Implemented batches pending integrated acceptance

- Provider-default selection retains missing/inherit, null/default, and explicit variant intent through settings, drafts, queue capture, retry, and history. Native request serialization uses the verified empty variant; existing explicit choices remain intact.
- Empty active reasoning becomes a non-expandable status. Completed empty reasoning has no blank control, and available text is visible while Markdown loads. Focused interactive reasoning checks and all ten screenshots passed review in `.cache/qa/reasoning-TsgCKF`.
- Strict matrix configuration, a realistic isolated Git fixture, external project graders, and managed-task/compaction evidence graders are implemented. These graders do not establish that live scenarios have run.
- History rendering now reads the virtualizer's current row set and restores prepend anchors synchronously through the virtualization threshold. Explicit reasoning disclosure state survives desktop/mobile remounts in a bounded cache. The full current fixture probe covers loaded older history, six viewport/theme combinations, queue capture, permission/question controls, rejected-send rollback, reconnect, and empty/delayed reasoning.
- Canonical history restoration reads `user.model.variant`, with the legacy top-level variant as a compatibility fallback. Native compaction user records cannot replace the latest actual user's model/effort choice. A real Builder manual-compaction preflight reproduced Default changing to Medium after the first boundary; the correction passes adversarial Default/High synthetic boundaries, reload, and the next actual UI send. Live two-boundary re-verification remains required.
- Saved plan storage now hashes only project-ID path components that exceed the filesystem's 255-byte limit. Valid existing storage paths and public project IDs remain unchanged; shared web/Electron contracts pass their focused checks. This fixes the reproduced long-project-path `ENAMETOOLONG` failure in the plan revision API.
- The managed Slim wrapper now accepts the installed server descriptor export. A narrowly fingerprinted managed Meridian patch uses Bun's native HTTP server for its unchanged Hono app, preserving cancellation disconnects; the actual Anthropic UI cancel/reconnect journey passes. User-managed packages and native session-history conflict checks remain unchanged.

## Candidate and evidence boundaries

The latest preserved web candidate is `.cache/qa/candidate-web-dOSUpt`, manifest SHA-256 `a2435d2921e5169fed8fc51b51564fa6d4ae1ee7ebe3482dd80014230094e0fd`. Its startup graph is 4,652,876 raw / 1,363,180 gzip bytes under Node 26/zlib 1.2.12, below the unchanged web limits with 32 startup chunks. It includes the optimistic-variant, sticky-anchor, landscape-sidebar and mobile-control corrections below. The bundle command still exits nonzero for the excluded VS Code overage (4,121,108 raw / 1,189,059 gzip bytes); web passes independently. A Bun compression report is retained separately; it cannot be compared numerically with the Node baseline. The isolated three-build resolver comparison is documented in `startup-bundle.md`; its byte reduction does not establish whole-application CPU savings.

Actual Electron matrix cells use locally built unsigned packages with archived source/UI identities, unchanged unpacked SQLite/PTY hashes, native smoke results, and proof that the packaged main process booted. The current candidate is `packaged-electron-NT1qd8`; its preserved initial-UI baseline is `packaged-electron-qSvPmN`. Their actual archives, 919 server/shared files, seven shell/bootstrap files, compiled main and three native artifacts are byte-identical. The served `Resources/web-dist` is verified separately and retains each arm's exact original UI. Both source manifests honestly record the current working tree. The earlier cxsg0V/7PWx3R packages remain unchanged. Refreshing the baseline also changed 232 entries in its unused embedded `node_modules/@openchamber/web/dist` copy; the complete list is retained, and the bootstrap/main/server path selection proves that the app serves the explicit Resources path. Development Electron runs are not substituted for packaged acceptance. Both web and Electron cells reject source or artifact drift. Effective controls evidence observes the final native `chat.params` hook before provider adapters; it does not claim provider wire capture or access to hidden reasoning. Package review: `test-refresh-package-pair-independent-review.json` in the audit cache.

Several earlier broad matrix attempts used superseded candidates or failed in QA startup/control targeting before making provider requests. Their partial results are retained as diagnostics, not combined into a final matrix pass. The final planned set contains 48 core transport runs (three providers × two primary roles × Plan on/off × Default/High × web/Electron), 24 explicit-High project-work runs, 12 Default manual web compaction runs, and 12 Default natural Electron compaction runs. These 96 scenario runs cover 48 logical selection cells; the separate project-work runs ensure that High also receives meaningful edits, plan approval, independent browser grading and managed-task acceptance. Both compaction adapters require two native boundaries and resumed project/domain/API/browser behavior. Natural acceptance keeps native thresholds unchanged and rejects provider-overflow substitutes.

The host-stall investigation identified synchronous package-manager probes in the automatic update check. A native Node CPU profile linked the pause to `checkForUpdates` and `detectPackageManagerDetails`; the slow bridge calls waited before host dispatch while their handlers completed quickly. Automatic detection now uses bounded asynchronous probes and single-flight caching, preserving the existing selection precedence and synchronous CLI API. One targeted before trace and one after trace reduced time in synchronous package-manager probes from 4,358.7 ms to zero and maximum observed event-loop lag from 3,423 ms to 147 ms. These are diagnostic source-host observations with identical UI, not repeated or whole-application performance measurements; the final UI pair intentionally shares the corrected backend and does not independently measure this change. The exact pre/post module sources were recovered against the trace provenance for separate repeated verification. A subsequent xAI UI core journey passed send, reload, cancellation, reconnect, and explicit High controls. An earlier cancellation probe never submitted its second request, so its cause remains unproven.

The updater-corrected package pair uses the same backend archive and unpacked native SQLite/PTY files for the candidate UI and preserved initial UI. Native Electron smoke checks passed. A final-validation failure exposed an outdated test that prohibited the newly required Default option; its correction changes only `ThinkingVariantLabels.test.ts`. The resumed deterministic gate passed. Two manual-compaction runs already in progress retain this test-only source-drift qualification and serve as functional preflights, not final identical-source matrix passes.

Both manual preflights observed two linked native summaries, independent ordered lifecycle events, unchanged saved plan and paused repository, and retained Default selection. Builder's implementation phase then exceeded its total ten-minute scenario limit; its last completed native test passed, but external application verification did not run. The final manual configuration uses a forty-minute total limit. Orchestrator retained the exact pending task identities and dispositioned them after its first boundary. It then targeted an intentionally disabled plan: its intervening human Plan-mode request opened a newer pending revision, making the earlier card superseded. The actual fixture UI reproduces the disabled state and explains the targeting timeout; no stale-plan safeguard change is needed. The implemented approval helper presents the unchanged saved project plan for a fresh approval source after its exact native-boundary preservation checks; final live verification remains required.

Orchestrator's gap-free journal separately proved a product issue: Plan was enabled on all three initial human turns but absent on the continuation after reload. Canonical Plan restoration now reads the latest actual user choice, ignores native compaction and automatic-continuation records, and preserves an explicit in-memory off choice. Implement Plan remains an authoritative off action. The fix passes 79 focused tests and the full 3,465-test UI suite, lint, and type-checking; integrated fixture and live verification remain required. QA now requires the actual Plan instruction rather than accepting a textual implementation pause as equivalent.

A subsequent controlled UI reproduction proved that a late agent-catalog refresh replaced a draft's visible agent/model with account defaults while Send retained the draft's original selection. Catalog completion now reuses the existing authoritative draft-restoration resolver, reading the currently selected draft at completion. The old and corrected builds were exercised through actual controls, catalog refresh and fixture submission; all six screenshots were reviewed. The corrected build retains matching Builder/model/effort values throughout. Three asynchronous regressions, the standard full UI runner, 122 send tests, UI type-checking and lint pass. Evidence is recorded in `draft-catalog-fix-validation.json` under the audit cache.

Cold native initialization also exceeded the QA driver's initial thirty-second composer wait. A bounded diagnostic observed config/provider responses taking 111–118 seconds after low-level health had become ready; no particular plugin is established as the cause. Only initial bootstrap now has a bounded 180-second gate requiring usable controls, initialized UI stores and exact connected model/agent catalogs. Ordinary reload checks retain thirty seconds. The new gate passed an actual startup-only run on UhVNFX without provider prompts, with zero journal gaps and clean process shutdown.

The TrzdD4 manual Builder preflight passed two native boundaries and paused-state checks but stopped during implementation after a denied `/Applications/*` read. The agent had been asked to exercise a browser that the isolated web profile did not expose. The fixture now assigns native tests, HTTP checks and restart persistence to the agent, and explicitly assigns browser presentation to the mandatory independent browser grader. That interrupted run has no GREEN or finished-application acceptance. Its initially wrong draft label, precise denial correlation, and source/runner changes remain explicit qualifications.

The first xAI automatic boundary was observed at 468,307 tokens against the unchanged 468,000-token native threshold, with `auto:true`, no overflow and a linked native lifecycle. Its summary retained revision 2, the exact saved-plan path and the implementation pause. The continuation nevertheless used glob searches that returned no files, inferred the plan was missing, and asked for guidance without directly reading the retained path. The actual plan hash was unchanged and the journal had no gaps. This is a retrieval/inference failure after surviving context; summary or record loss is not established. The unanswered question was preserved. The three matched pairs reported below subsequently exercised this symptom and did not reproduce unresolved missing-plan inference.

The final cxsg0V package passed native attachment selection through the macOS chooser, chooser cancellation, the native File → Add Workspace action and dialog cancellation, and Quit confirmation's Cancel, Wait and Quit Now paths. Its one credential-free fixture prompt preserved all 700 bytes of the text requirement and the exact 1,095-byte PNG digest in the sanitized journal; raw and canonical selection agreed on Low. All five archived renderer PNGs and the inline native chooser/quit screenshots were reviewed. The submitted capture already shows a completed response and is not claimed as in-flight visual proof. The disabled-Bot QA profile reports unknown scheduler/checkpoint status in the quit dialog, so this establishes that confirmation path without claiming active-Bot behavior. Quit Now exited with code 0. Production, runner and initially pinned package-evidence hashes stayed unchanged; console/cleanup errors are empty. Retained process identities were audited both before and after fixture closure with no survivors. The 35-record journal has zero gaps. Evidence: `.cache/qa/native-acceptance/native-chooser-quit-Oi3VJf/visual-review.json`. Earlier pHrgLt and j7qNCw native results remain preserved as superseded evidence.

A native first-send check exposed a Low → Default → Low composer-label transition. A bounded ten-second canonical-user delay reproduced it on UhVNFX while preserving the actual first request at Low. Contrary to the initial code-based queue concern, both observed queue captures retained Low and the second dispatched request was also Low. All five screenshots and the gap-free journal were reviewed. The correction includes the captured variant on the real optimistic user row and both normal/slash insertion paths; null uses the typed canonical empty-string default representation. The corrected OHB9Gj UI retained Low in all forty delayed-echo samples, the queued capture, canonical restoration and the second request; its five screenshots and 31-record gap-free journal were reviewed. Those production changes are included unchanged in XmxL0y. No wrong queued transport was demonstrated. Evidence: `.cache/qa/adapter-probes/variant-delay-uzRMUp/` and `variant-delay-Zj9fhl/`.

Three packaged interactive-performance protocol smokes reproduced a 40 px anchor shift on the final older-history page; earlier pages stayed within 0.5 px, and the journal had no gaps. A cache-only scroll-write trace showed that restoration did write its correction, but the user-message element remained clamped at zero by its sticky ancestor. The earlier metadata-retention attempt did not fix the failure and was removed. Anchor capture and restoration now use the explicitly identified normal-flow owning turn for user messages, retaining exact message geometry for assistants. Packaged 52YzDJ passes the unchanged 2 px gate for both 360-message histories: all eight pagination steps stay within 0.5 px, and both final pages shift 0.125 px. The eleven-record journal has no gaps. This short protocol smoke then stopped at a session-switch assertion and does not establish results for the later performance phases. Evidence: `.cache/perf/2026-09-05T08-01-23-989Z-interactive-protocol-smoke-4/`.

A later cache-only interactive-driver diagnostic completes all 148 original actions on ge9ym7: eight zero-shift pagination pages, exact canonical session switches, eight reasoning open/close checks, cancellation/reconnect uniqueness, forty navigation actions and the final retained draft. The earlier reasoning gate had used a completed 0.2-second singleton, which correctly renders inline under the existing 15-second rule. The diagnostic uses an actual 16,203 ms singleton with 768 bytes of nonrepeated content, preserving all action/layout gates. Its 45-record journal has no gaps or errors, only two recorded optional-config HTTP 404s; owned process and fixture cleanup complete. This uses an explicitly identified cache-only driver override during the frozen study, so it is structural diagnostic evidence, not final performance timing or original-runner acceptance. Evidence: `.cache/perf/2026-09-05T08-59-17-906Z-interactive-cache-duration-diagnostic/`.

At 844×390, the actual long sidebar title overlapped its two hover actions by 2 px. Reserving sufficient trailing space now leaves a 10 px gap in both themes; both existing 20×20 actions remain visible and hittable. The before/after screenshots were reviewed in `.cache/qa/adapter-probes/landscape-hover-1VzfDq/visual-review.json`. This resolves the specific F14 crowding defect; full rich mobile states and physical-device keyboard behavior have separate coverage requirements.

A trusted mobile tap exposed a separate defect: pointer release opened the agent picker, then its synthesized click selected Orchestrator five milliseconds later. Activation now occurs on click, with the existing footer focus-preservation pattern. The corrected PvUPsj build's native-event trace shows the click remaining on Builder and then opening the panel; Builder/Low stays selected, including keyboard entry and Escape. That exact fix is preserved in VoC1qS. Mobile models with at most six effort options also had no reachable Default action: their inline chips offered only explicit efforts, and only larger lists exposed the full panel containing Default. Generic model rows now include the same existing null-capturing Default action directly. The actual VoC1qS browser submits High followed by the native empty-string Default representation and restores Default after reload. A subsequent mobile-to-desktop resize reproduces an independent defect: explicitly selected Low becomes Default when the controls remount, and the next real request carries the wrong native empty-string Default value. The before/after screenshots, transition samples and gap-free journal agree. A second run at constant 390×844 independently reproduces Low → Default after switching A → B → A, with the wrong Default on the next actual send. Settings navigation preserves Low because that view keeps the chat mounted. A cache-only correction tracks the last successfully restored exact canonical choice per session, leaving genuinely new choices eligible and preserving newer unsent selection; its 17 focused state tests pass. The per-session correction is now integrated in 4ZmpkJ with the same permanent-deletion lifecycle and no reactive marker updates. Its full focused batch passes 48 tests/206 assertions, file lint and UI/web builds pass, and pHrgLt packaging passes its native SQLite/PTY smoke. Actual after-build configuration regression checks remain required. Full six-state mobile acceptance remains open; no Cursor control policy changed.

The frozen xAI retrieval study completed all three fresh pairs in the predeclared control/compacted, compacted/control, control/compacted order. Every compacted arm successfully read its exact saved revision-2 plan; all six preserved the plan and implementation pause, without repeating completed source reads or failed verification during the diagnostic pause. Five arms passed. The final compacted arm remains failed because it raised an unanswered native plan-approval question instead of settling the neutral probe; it had already read the plan successfully. One other compacted arm retained a rejected PNG-as-text read without retry. All outcomes and generated-plan differences remain in the evidence. Across 4,862 reviewed journal records there are zero gaps, and all 41 run screenshots were reviewed. Source and runner hashes stayed identical. A separate OS audit found six study-owned native OpenCode parents and 30 language-server descendants despite successful per-run cleanup reports. Those 36 exact owned processes were closed and the final PID audit is empty; residual recreated files and the original false-clean reports are preserved. The runner ownership defect is being corrected before further live runs. Evidence: `.cache/qa/diagnostic-VoC1qS/retrieval-xai/study-review.json`.

These three pairs do not reproduce unresolved missing-plan inference and do not justify new retrieval instructions or persistent memory for that reported symptom. The approval-question difference remains an observed probe failure. This limited manual-summary diagnostic establishes neither general retention nor natural compaction or automatic-continuation acceptance; those operational checks remain separate.

The scoped source-test review is in `source-test-review.md`: three duplicate source assertions were removed because an existing real ManagedTaskList render test checks the same properties; all 55 scoped tests pass. The canonical packaging/path review is in `packaged-instruction-review.md`. Role and task contracts remain intact, and personal project configuration and legacy Tauri assets are preserved.

The actual native xAI preflight contains 27 finalized canonical reasoning parts. Two have exactly the existing 203-character ASCII-ellipsis fingerprint and visibly end mid-word; the other 25 range from 24 to 2,745 characters and are not suppressed by that predicate. The narrow rule is retained, with its existing tests for unfinished parts, other providers, adjacent lengths and Unicode ellipses. This is evidence about exposed adapter output, not provider wire content or an available missing tail. The journal has no gaps. Review: `.cache/audit/coding-agents-2026-09-05/xai-reasoning-suppression-review.json`.

The preceding VoC1qS production candidate passes `bun run validate:full`, including repository lint, all package type-checks and the complete test command. The main UI batch passes 3,465 tests and the server suite passes 3,593 tests; isolated UI suites and compatibility checks also pass. Log: `.cache/audit/coding-agents-2026-09-05/mobile-default-full-validation.log`. An intervening run failed an unchanged Bot egress response-bound test with `ECONNRESET`; its focused rerun and this complete rerun passed without changing that module. The latest 4ZmpkJ UI correction and QA-driver refinements receive focused checks and remain subject to a fresh full gate after the helper changes settle.

## Acceptance remains open

The final build/package, applicable native checks, fixture visual review and quiet full gate are complete. The original fixture focus-check failure did not reproduce in the observed follow-up; its precise cause remains unproven. Two separate xAI Orchestrator Default/Plan-off feasibility runs (manual web, then natural Electron) must establish the actual native task/summary boundaries and final application behavior before the 96 final live scenarios. Neither feasibility run counts toward that matrix. Quiet package measurements still require 24 resource runs, six session-memory runs and six interactive runs, plus six cold update-check hosts. Startup is measured within those package launches. Earlier smokes and microbenchmarks do not replace these 42 runs. No persistent memory system has been added; that decision remains conditional on reproduced operational compaction losses and matched controls.

Detailed logs, screenshots, journals, and provider metadata remain in ignored `.cache/`. Final reviewed outcomes will replace this progress section.

The unwrapped bounded H79wTt mobile run on 4ZmpkJ passes nine checks: High→Default/reload, separate actual Low sends after responsive remount and A→B→A, and all six rich viewport/theme states. Raw submission and canonical/journal effort agree on Low against independent Default baselines. All 73 unique PNGs are opened and hashed; 165 journal records have zero gaps. Both themes preserve 36px coarse and 20px fine session actions with a 10px title gap, zero-sized invisible anchor and title-owned leading glyphs. The effective driver files stayed unchanged. This is bounded correctness proof, with full matrix acceptance still false. Its stale generic live-study concurrency label is corrected in the review: the retrieval study had ended; unrelated source/helper work was concurrent.

A separate zero-send check yBRU7s proves a workspace-heading overlap at 844×390: the final c/e title glyphs hit New Worktree, and Project Menu/New Draft boxes overlap by 10px. The same Project Menu/New Draft overlap also occurs in actual 390×844 and 768×1024 drawers, measuring 12.61 px. The new dOSUpt/cxsg0V candidate reserves header space from the rendered action count and separates the trailing New Draft target throughout coarse web layouts. Fine-pointer and Electron spacing retain their prior values. Source lint, UI/web builds and native packaging smoke pass. The bounded odFHOw after-build check passes all three coarse layouts: title glyphs own their title control, 36px targets are separated by 3.59–4px, and the trusted Project Menu action opens/closes without activating its neighbors or changing observed draft state. All nine header/menu PNGs were reviewed. It sends no prompts and begins with a blank draft; rich active-session and dark-theme checks remain assigned to the full fixture matrix.

The frozen 4ZmpkJ full gate passes lint, all type-checks, all 408 script tests and the preceding package/UI suites, but fails one unchanged server worktree-route test (expected HTTP 200, got 404; 3,592 other server tests pass). The exact existing test file passes 3/3 in isolation. Its fresh mocks, per-registration library cache and synchronous route registration do not explain the full-suite-only result; contamination is plausible but unproven. The failed full run is retained, and a new full gate remains required. Evidence: `session-choice-coarse-full-validation.log` and `worktree-operations-failure-review.json` in the audit cache.

The focused update-check benchmark is now installed with ten passing helper tests and scoped lint. It runs six real cold hosts in AB/BA/AB order with exact pre/post module hashes, ordinary read-only discovery, health latency, event-loop delay, CPU attribution and owned-process audits. It remains unrun until the final quiet performance window. Fresh-checkout module recovery and the importable Node invocation are documented in `docs/QA.md`.

The first frozen dOSUpt full gate passes global lint and package type-checks, then stops with 427/428 script tests passing. The unchanged recursive-child cleanup test at `scripts/agent-evals/client.test.mjs:1101` receives the expected evaluation timeout but fails its cleanup-complete predicate. Its 30ms turn deadline and 100ms cleanup budget make load sensitivity plausible; the original failure log lacks the cleanup fields needed to identify the exact cause. The existing focused test passes without edits. Concurrent fixture/native QA is recorded, and a quiet full rerun remains required. Evidence: `workspace-heading-full-validation.log`, `recursive-cleanup-focused-rerun.log`, and `recursive-cleanup-failure-review.json` in the audit cache.

The first final nine-cell fixture attempt completed on frozen dOSUpt/cxsg0V: all four Builder web/Electron Default/High cells and the rich mobile cell pass; all four Orchestrator cells fail before core prompts because their UI selects gpt-5.5 outside the single-model fixture catalog and has no effort trigger. The private fixture profile omitted application model defaults and agent selections; captured profile fields and the actual controls corroborate that setup defect. Original outcomes and screenshots remain unchanged. All nine journals have zero gaps, all fourteen owned process roots and their observed descendants are gone, and every private runtime directory was removed. The corrected private fixture will pin its application model and inherited Low selection, with an exact provider/model/agent check before any send. The same nine cells will run again under a new runner identity. No live-provider acceptance starts from the partial result.

All 285 physical screenshots from the original nine-cell attempt were individually opened; a separate completeness audit verifies every file hash and exactly one review per image. The mobile result contains 137 of those screenshots, but content sanitization redacted some long filename references in its JSON manifest. Review uses the physical files without rewriting the original result. The QA-only serialization correction preserves only successfully written, validated relative PNG basenames from the screenshot callback; all other evidence retains the shared sanitizer. Review also found offscreen Plan controls in otherwise coherent screenshots, so the driver must reveal and verify the exact current or disabled historical approval control before capture. Evidence: `.cache/qa/final-fixture-dOSUpt/original-nine-visual-review-completeness.json`.

The quiet r2 full gate runs without competing QA, native, provider or performance hosts. It reaches the complete 3,593-test server suite and fails `routes.integrate-policy.test.js` (expected HTTP 403, received 404) and the `db-maintenance.test.js` worker-thread case (expected 56 deleted events, received 92; maintenance otherwise reports success). The earlier recursive-child cleanup and worktree-operation tests pass in this run. The exact causes of these new failures are not yet established. The complete failed result remains in `fixture-profile-r2-quiet-full-validation.log`; focused follow-ups cannot replace it.

The database failure subsequently reproduces in isolation: the fixed September 4 fixture's supposedly active session crosses the real worker's 24-hour retention cutoff on September 5, accounting for the extra 36 deleted rows. The worker test now builds timestamps relative to its real clock; deterministic in-process cases retain their fixed clock. The expected 56 deletions stay unchanged, with additional assertions for one pruned session and all 100 active-session events retained. All 26 cases pass. The Git files pass 5/5 in isolation; their original 404 cause remains unproven. Bounded failure-only request/route/principal and response diagnostics preserve every existing assertion without changing route policy. These three test files are the only source changes between cxsg0V and NT1qd8, and the candidate archive enumeration confirms exactly those changes. Main, preload, QA bootstrap, Electron framework/executable, native binaries and served UI stay byte-identical, so Oi3VJf's native evidence applies to those unchanged behaviors without being relabeled as a new run.

After refreshing both packages, `bun run validate:full` passes from frozen source `f787a1f140317cf4db1ca6e09b03b6c0b63bce4447b6f427e930bfff4c1cae12` and runner `efabcfe01caf0c1297500592c1fa1e7a1a2219b6915384efe0d56e578df56d25`. The complete quiet result is in `test-clock-git-diagnostics-quiet-full-validation.log`; the earlier failures remain preserved.

The corrected r3 fixture matrix completes all nine planned cells: four web core cells, three Electron core cells and the rich mobile cell pass. Electron Builder Default stops at `focusable fixture tool disclosure` after its intentional failed-tool turn has completed and become idle; later checkpoints are unobserved. That failure's saved journal has 200 records and no gaps. Across all nine runs, source, runner, UI and package identities match; journals have no gaps or parse failures, private runtime directories are removed, and the retained OS identity audit finds no remaining owned processes. Mobile passes 40 checks and its 137 screenshot filenames match the physical files. Several otherwise passing core screenshots omit the intended disabled historical Plan button despite a preceding correct DOM sample. Without geometry and scroll samples at capture time, the cause remains unproven and those images provide limited evidence. The original outcomes and images are retained; focused follow-ups will not turn the original 8/1 result into a 9/0 claim.

The final live master configs are preserved in `.cache/qa/final-matrix-configs-dOSUpt-r3`. Separate launch configs in `.cache/qa/final-cell-configs-dOSUpt-r3` map one-to-one to all 96 original runs, preserving every selection, scenario, repetition, run ID and timeout while assigning unique evidence roots. Their receipt verifies 48 core, 24 project, 12 manual and 12 natural runs, with 32 per provider. Before each OAuth/Claude access launch, fresh canonical credential metadata must cover the configured timeout plus ten minutes; an expired or insufficient access token cannot be admitted. API-key access retains the explicit expiry-check exception described below. These prepared configs establish no live result.

Two supplemental original-cell replays use the same frozen r3 runtime and runner with a cache-only read-only CDP observer. Electron Builder Default oLFF8x and Web Builder High frrGkP both pass all 25 checks; all 72 screenshots were individually reviewed and rehashed. The Electron failed-tool and recovered-tool stages record the original collapsed-group and focus predicates returning true, followed by actual Enter and readable output. Both replays capture the exact historical Plan button with stable geometry and scroll position through the predicate, delay and capture samples. The added reads take roughly 353 ms and 382 ms cumulatively and perturb timing, so these passes cannot establish why the earlier captures or focus check failed. All source, runner, package, UI, wrapper and original-matrix hashes remain unchanged; journals have no gaps and owned-process audits are empty. Shared sanitization removed some trace hash strings; capture ordinals map to the original screenshot sequence and independently hashed physical files without reconstructing the removed values. Original results remain unchanged. Comparison: `.cache/qa/r3-supplemental-observer/comparative-capture-review.json`.

The r4 QA-only admission change checks the selected OAuth or Claude access actually copied into each fresh live profile, immediately before any host starts. Its expiry must cover the full configured timeout plus ten minutes; unavailable or insufficient copied access fails through normal cleanup. Existing OpenAI/xAI API-key support remains available with an explicit indication that the expiry check does not apply; no lifetime guarantee is inferred. All 13 focused profile tests pass. Source `f787a1f140317cf4db1ca6e09b03b6c0b63bce4447b6f427e930bfff4c1cae12`, served UI and packages remain unchanged; only runner identity changes to `5b9041364365ea8cac70a4fc321d80e740c1038f75d3a34b359e48ce75ff9a31`. The new quiet `bun run validate:full` passes with 441 script tests and the unchanged full application suites. Evidence: `credential-admission-r4-full-validation-receipt.json` and its complete log in the audit cache. R4 master and individual configs preserve the exact 96-run mapping; earlier configs and evidence are unchanged.

The four remaining original-cell capture follow-ups complete on the unchanged r4 runtime: Web Orchestrator High 5UKEei, Electron Builder High Td4PV1, Electron Orchestrator Default 4nliP0 and Electron Orchestrator High 6ubUTX each pass 25 checks. All 144 screenshots are individually reviewed and rehashed; every historical Plan target is fully visible. Matching DOM and functional evidence verifies its disabled state and expected explanation. The observer bytes remain identical to the earlier two replays; its new wrapper changes only the four-cell allowlist and frozen-receipt path. Across all six supplements, 150 checks pass, all 216 images are reviewed, and 9,564 journal records have zero gaps. The complete original/supplemental set contains 618 reviewed physical images. A final OS check of 464 retained process identities across all fifteen runs finds no survivors. The selected nine-cell coverage uses the original two unaffected web cells and mobile plus the six supplements, totaling 240 checks and 425 images. Each cohort retains its own runner identity; the original 8/1 outcome remains unchanged. This completes fixture coverage with the declared observation limits, not live-provider or performance acceptance. Closure evidence: `.cache/qa/r3-supplemental-observer-v2/six-supplemental-fixture-coverage-closure.json`.

The single r4 xAI Orchestrator Default/Plan-off manual web feasibility was released after that closure and a fresh source/runner/UI/package rehash, then launched at approximately 13:26:50 UTC. Its configured limit is forty minutes; copied-access admission must cover that interval plus ten minutes. It remains excluded from the 96-run matrix and requires the exact two-boundary operational and application checks before acceptance. Its Plan-off composer approval cannot establish the separate fresh Plan-card path. Root launch receipt: `manual-r4-root-launch-receipt.json` in the audit cache.

Manual feasibility UeliFc closes failed after eight passing checks. Revision-2 pause and exact mixed-cohort readiness passed; the first linked 5,022-byte native summary completed, but the recorder had already filled its 500-snapshot limit. The preserved entries contain only seven distinct task projections and 493 adjacent duplicates; concurrent readiness polling and the outer observation loop bypassed unchanged-state coalescing before the mixed cohort was ready. The final retained read ends 10,139 ms before native compaction starts, leaving no recorded after-start bracket. The archived journal has 1,681 parseable records and zero gaps. Historical task events corroborate activity but cannot replace those missing authoritative samples or turn the result into a pass. All four PNGs are reviewed; all 65 retained process identities are gone, the five-session cleanup and project archive are complete, and the frozen source/runner/UI/package hashes remain unchanged. The failed project and original evidence are retained. A bounded QA recorder correction is under review; native boundary 2, result collection, approval, repair and independent application/browser checks remain unobserved. Evidence: `manual-r4-recorder-cap-review.json`, `manual-r4-owner-closure.json` and the UeliFc run directory.

Independent saved-evidence review verifies that UeliFc's final real project plan is byte-identical to its 10,588-byte revision-2 baseline. Its separately sanitized archive has a different, explicitly recorded hash; that is not runtime plan drift. The summary text nevertheless calls the plan revision 1 and asks to reconcile revision 2, and describes one task marker as truncated. These content limitations did not cause the snapshot rejection, and no subsequent operational retrieval or implementation test ran. They do not establish a failed repair or justify a memory/instruction change. The complete failed-run classification, raw-input hashes and remaining unknowns are preserved in `manual-UeliFc-failed-feasibility-closure.json` and `manual-UeliFc-independent-journal-review.json` in the audit cache.

The r5 QA-only recorder stores immutable task projections once and retains every exact read interval in completion order. It preserves both polling cadences and decodes to the unchanged graders' input shape. Hard limits remain explicit: 500 projections, 256 stage names, 100,000 read references and an 8 MiB minified JSON budget; the separate coalesced action evidence has 500 rows and a 256 KiB minified budget. The original 500-entry prefix replays exactly as seven states across three stages, using 24,727 rather than 1,077,286 bytes. Its original missing boundary still fails; no missing reads are synthesized. Adversarial ordering, nearest unavailable/consumed samples, limits and legacy decoding pass focused tests. The quiet full gate passes with 454 script tests. Production source, served UI and packages remain unchanged; runner identity is now `24e52f10eb424b24f225b6ad368c9195693d2dc020f3ba318dc85dfac980b36f`. Evidence: `compaction-recorder-r5-full-validation-receipt.json`, `compaction-recorder-correction/final-validation-receipt.json` and `final-dOSUpt-r5-source-freeze.json` in the audit cache.

The first original r5 baseline performance member completes 63 actions before timing out on the third older-history page. Its trusted click and correlated HTTP 200 response are retained, but the failed height-growth/anchor-presence predicate did not save its operands. The later 2 px anchor assertion never ran. The trace remains complete, the journal has no gaps, both PNGs are reviewed, source/artifact integrity passes and all owned processes are gone. The initial broader classification as a clean baseline correctness failure is explicitly superseded by an unknown UI-versus-predicate classification. The other 41 planned members remain held; no aggregate or candidate comparison exists. Evidence: `.cache/perf/2026-09-05T14-32-15-225Z-final-interactive-r5-baseline-1/`, including `classification-correction.json`, and `interactive-r5-baseline-1-qa-predicate-review.json` in the audit cache.

The first separate uncounted pagination diagnostic stops before its workload at the original startup visibility gate. Its final document is complete with the fixture controls, but remains `hidden`; no pagination action, observer installation or sample occurred. The reviewed renderer PNG shows the loaded app and cannot establish native foreground, minimized or occluded state. All 44 owned processes are gone, the three-record journal has no gaps, and source/artifact integrity passes. The existing startup path does not issue `Page.bringToFront`; it shows and focuses the window without recording native activation state. Why this document stayed hidden remains unproven. First diagnostic closure: `.cache/perf/pagination-timeout-diagnostic-r5/closure-review.json`.

The second separate diagnostic adds one `Page.bringToFront` from an already-returned DOM-ready-but-hidden sample. Its original full readiness predicate and timeout remain unchanged. The command is acknowledged, but all 323 wholly subsequent samples remain hidden through 37.574 seconds after acknowledgement. No pagination workload runs. The one PNG is reviewed, the three-record journal has no gaps, and all 32 retained process identities are absent. Native visibility cause remains unknown; acknowledgement did not establish foreground visibility. Closure: `.cache/perf/pagination-timeout-diagnostic-r5-v2/closure-review.json`.

The third separate diagnostic passes startup without either the conditional CDP foreground command or a native action. It reproduces the third-page timeout and captures its operands: the transcript initially has 50 message elements and an 8,548 px height, then remains at two elements and 641 px with the original anchor absent in all 149 later samples. The selected session and visible document remain unchanged. The already-requested history response returns HTTP 200 with 200 canonical message rows. This establishes a rendered baseline history failure; it does not establish canonical-store data loss or the internal virtualization mechanism. Added samples are not atomic with the original animation-frame checks and affect timing. The original baseline member still lacks these operands, and no diagnostic replaces it or contributes comparison metrics. Independent interpretation: `pagination-v3-independent-dom-interpretation.json` in the audit cache; exact operands: `.cache/perf/pagination-timeout-diagnostic-r5-v3/root-operand-review.json`.

After the V3 diagnostic closure, original baseline 2 repeats the page-3 timeout after 63 passing actions. It passes startup, frozen integrity, trace and journal checks; both images are reviewed and all 31 owned process identities are absent. Original baseline 3 then fails startup before workload: the final saved snapshot is complete but hidden with its controls and model/catalog ready. Its native-window state and the cause of hidden visibility remain unavailable. Its sole PNG and three gap-free journal records are reviewed. Cleanup escalates to SIGKILL, followed by absence of all 24 recorded process identities. The unchanged R5 stopping rule leaves all three candidates, 24 resource members, six memory members and six backend members unrun. No replacement or complete interactive aggregate is emitted. The exact retained member references and separate diagnostics are recorded in `final-interactive-r5-cohort-summary.json` in the audit cache; operational compaction and the final live matrix remain open.
