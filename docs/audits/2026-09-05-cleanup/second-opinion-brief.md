# Technical brief for Fable 5.1 High — 2026-09-05

This is an advisory review requested by the user before continuing the implementation. Criticize assumptions and solutions; the coordinating agent retains final judgment and will assess your rationale against repository evidence. Read-only file inspection and bounded `git diff`/`git status` are allowed. Do not edit files, run tests/builds/programs/servers, contact providers, or inspect another repository. The only repository in scope is DevRyan. The working tree contains concurrent work; do not attribute its entire diff to this audit.

## Goals and current stage

The user wants a correct, fast, responsive Coding Agents experience with lower system load. Responsiveness, speed, CPU, memory, startup, rendering and backend overhead are first-class optimization goals, alongside thinking correctness and compaction continuity. Functional acceptance alone does not satisfy those goals.

Complete acceptance for Builder and Orchestrator across Electron, web and responsive mobile web; Plan must be an orthogonal mode; evaluate provider-default and explicit thinking. Use real multi-file work, independent graders, text/image requirements, a preserved user edit, mid-task revisions, rejected approaches and authoritative child/result reconciliation. Test native manual and natural compaction through two successive boundaries and subsequent correct work. Do not implement global memory speculatively. Bots, VS Code runtime QA, release publication and other repositories are excluded.

The latest scope is one main model per provider: OpenAI `gpt-5.6-sol`, Anthropic `claude-sonnet-4-6`, xAI `grok-4.6`. Additionally investigate Codex 5.3-Spark as Explorer because the user reports problems with that assignment. Exact Spark routing and its incident have not been verified yet. This is not automatic fourth-model broad coverage.

Implementation is substantially complete and validated, but integrated acceptance is open. The current full gate passes, including 454 script, 3,468 UI and 3,593 server tests plus lint/types and other checks. The current packaged Electron candidate passed startup and all 148 deterministic interactive actions; all four screenshots were inspected, the full trace parsed, the journal has zero gaps/errors, and 20 recorded processes are absent. Cleanup included recorded SIGKILL escalation. This is one functional run, not a repeated performance comparison or live-model acceptance.

Final live-provider coverage and complete two-boundary operational compaction journeys remain open. The existing 96 scenarios are our decomposition of required behaviors, not an immutable product requirement. Partial compaction evidence and three matched compacted/control pairs did not establish the suspected plan-retrieval loss; they do not prove broad operational retention. A recorder cap interrupted one live journey after the first native boundary. Snapshot storage was corrected and validated, but the complete operational retry has not run.

## Diff map and rationale

Inspect narrowly if needed. New helpers may be untracked, so ordinary `git diff` alone omits them.

| Area | Relevant paths | Intended behavior |
|---|---|---|
| Thinking capture and recovery | `packages/ui/src/sync/send-config.ts`, `selection-store.ts`, `session-ui-store.ts`; `packages/ui/src/components/chat/queuedSend.ts`; `packages/ui/src/lib/opencode/client.ts`; `packages/ui/src/lib/agentDefaultResolution.ts` | Keep missing/inherit, null/provider-default and explicit effort distinct. Resolve at enqueue/send capture; preserve through serialization, optimistic messages and restoration without changing personal configuration. |
| Reasoning output | `packages/ui/src/components/chat/message/parts/ReasoningGroup.tsx`, `ReasoningPart.tsx`, `reasoningDisclosureExpansion.ts` | Empty active reasoning is a status; completed empty parts disappear; real content remains accessible; expansion persistence is bounded. |
| History, Plan and draft state | `packages/ui/src/components/chat/MessageList.tsx`, `components/TurnItem.tsx`, `ModelControls.tsx`; `packages/ui/src/sync/session-plan-selection.ts`; `packages/ui/src/stores/useConfigStore.ts` | Render current history rows, preserve prepend anchors synchronously, restore authoritative choices and unsent intent, and finish model catalog restoration. |
| Mobile | `packages/ui/src/components/chat/MobileAgentButton.tsx`; `packages/ui/src/components/layout/MainLayout.tsx`; `packages/ui/src/components/session/sidebar/SessionNodeItem.tsx`, `sortableItems.tsx`; `packages/ui/src/styles/mobile.css` | Improve touch activation, resizing, provider-default access and nonoverlapping controls. Physical-device keyboard behavior remains unverified. |
| Performance | `packages/ui/src/sync/event-reducer.ts`; `packages/web/vite-chunking.ts`; `packages/web/server/lib/package-manager.js` | Avoid repeated history lookup, reduce the eager startup graph, and use bounded asynchronous update probes. Keep store reference stability and shared-runtime contracts. |
| Compaction evidence | `scripts/qa/compaction-snapshot-evidence.mjs`, `compaction-scenarios.mjs`, `natural-compaction-scenarios.mjs` | Intern repeated snapshot data while retaining every read and timestamp, original observation order, strict grading and explicit gaps. Never reconstruct unavailable boundary evidence. |

Concurrent SDK/version/provider-recovery edits in manifests, `bun.lock`, `packages/web/server/lib/opencode/version-policy.js`, `packages/web/server/default-config/user-profile/plugins/devryan-primary-recovery.mjs` and `packages/harness-runtime/lib/provider-recovery-policy.js` belong to another task. Preserve them and qualify attribution in shared files.

## Performance evidence and limits

- Earlier three-run reducer microbenchmarks showed one-session delta cost from 23.76 to 7.00 microseconds and four-session snapshot cost from 30.79 to 7.41 microseconds. These measure reducer cost, not perceived app latency or total CPU.
- Isolated startup-graph work reduced raw bytes by 7.97% and gzip bytes by 8.12%. The current web startup graph is below the original budgets; the excluded VS Code overage remains separate. Smaller bundles alone do not prove faster startup or less system load.
- Three-run test-memory measurements reduced title-case audit peak RSS median from 408.61 to 213.03 MiB, with slower elapsed times under that measured load. This is test-runner memory, not application memory.
- Repeated whole-app measurements remain incomplete. Original R5 stopped after two baseline pagination failures and one startup failure; 39 members remain unrun. The successful candidate run cannot replace failed baseline members or validate an incomplete aggregate. Historical diagnostics consumed disproportionate time. No baseline repairs or silent run substitution are permitted.
- Separate deterministic app costs from provider/network latency. Use startup, typing while another session streams, one/four sessions, history opening/pagination, switching, scroll/disclosure, cancel/reconnect, repeated navigation, post-close memory, backend probes and owned-process cleanup as relevant observations. Prespecify primary metrics, use identical conditions and three fresh valid baseline/candidate runs, and report absolute values, variability and limitations.

## Proposed next order to challenge

First investigate Spark Explorer from its journal and effective routing, with one bounded differential reproduction. Then complete representative manual/natural compaction journeys through actual subsequent work; expand into resumable three-provider acceptance batches only after those paths work. Run valid performance measurements in quiet blocks and finish the report. Available independent provider work need not wait on expired xAI access; renew only through the normal owner when that batch is ready.

The user has just stressed that responsiveness, speed and system load are also central goals. Challenge whether placing performance after all live acceptance would delay those goals unnecessarily. Independent performance work may deserve earlier quiet blocks; clarify the minimal dependency and how to avoid endless baseline diagnostics. Do not lower the original correctness or measurement criteria to make the schedule easier.

## Questions

1. What is the smallest Spark comparison that separates model behavior from Explorer routing, supported effort, permissions/tool availability, task-ledger lifecycle and parent reconciliation? Which variables must match the main OpenAI control? What evidence warrants expansion?
2. What would distinguish compaction-induced context loss from ordinary model mistakes, retrieval failure or recording gaps? What concrete result justifies retrieval repair, bounded pre-compaction continuity injection, or a persistent checkpoint? What counterexamples would invalidate our current reasoning?
3. Which observations can share realistic journeys without losing independently graded coverage? Preserve every required model/role/Plan/runtime and thinking behavior. Call out actual redundancies or hidden missing cross-products rather than recommending arbitrary count reductions.
4. How should we interleave functional, live/compaction and performance work to improve responsiveness and system load sooner? Give a concrete stopping rule for baseline or tooling diagnosis that preserves honest acceptance. Identify a minimal next measurement and why it is decision-relevant.
5. Which implemented design is most vulnerable to a subtle regression? Support criticisms with the brief or bounded source inspection, separate facts from hypotheses, and prioritize the next decision. Do not execute your recommendations.

Additional context: `resume-plan.md`, `macro-review.md`, `coding-agents-acceptance.md`, `performance.md`, and `.cache/audit/coding-agents-2026-09-05/approved-plan.md`. The most recent user model clarification supersedes the original plan's every-configured-model wording.

## Coordinator follow-up after your two reviews

Your source inspection found useful issues. Here are my provisional decisions; challenge the rationale, using the evidence already read. No further broad inspection is needed.

- Adopt earlier quiet performance work, with independently admitted workloads. Preserve stopped R5. Start a separately identified block using the existing backend AB/BA/AB probe and idle/streaming resource protocols, where their inputs isolate the intended comparison. Retain three fresh observations per variant. Do not require baseline pagination to admit unrelated workloads, and do not use resource metrics as proof of input or scroll responsiveness. A startup/integrity/correctness failure stops that affected block for classification; no automatic retry or additional observer layer. A historical failure prefix is not a complete interactive comparison. Any shorter shared protocol needs prospective definition, fresh runs, and explicit scope before measurement.
- Do not treat current base configuration as contradicting the user's past Explorer incident. We need its effective overlay, selected model, and correlated runtime evidence. Also, one differential pair cannot imply “model/adapter” versus “routing/ledger”: nondeterminism and shared task difficulty remain alternatives. Use the pair for localization and expand only when the journal and result justify it. I will not add three heterogeneous-provider cells mechanically; the focused Explorer case needs explicit per-role assignment and actual parent/result reconciliation.
- Our independent source review confirms both compaction coverage findings. There is missing manual Electron coverage, but the original wording does not explicitly demand 12 additional manual Electron runs. Compose meaningful manual UI/continuation evidence into selected existing Electron project journeys where possible. For natural Orchestrator, use an explicit phase policy: active witness at boundary one, completed-undispositioned witness at boundary two. Preserve native brackets, summary exits, exact identities, pending-envelope uniqueness and exactly-once collection. Move result collection until after its required summary-exit observation. Keep two genuine auto cycles and all gap/threshold/Plan checks. Do not manufacture or withhold a native result to satisfy a witness.
- The delayed-parts/manual-selection restoration concern is a testable hypothesis, not yet a reproduced bug. I am checking the existing selection guards. Source changes should invalidate evidence according to affected contracts and bytes, with final validation/refreeze; “every code fix restarts every matrix cell” is too categorical. Applicable unchanged evidence needs explicit justification. Do not commit the shared mixed working tree merely to tidy its appearance.

Please give a short final challenge (roughly 500 words): (1) any flaw in the per-workload measurement and failure policy, (2) the strongest falsifiable counterexample to the revised compaction policy or evidence reuse, and (3) what minimal additional observation would most improve the next decision. Correct or qualify any earlier causal claims that were too strong. Do not execute recommendations or expand the review further.
