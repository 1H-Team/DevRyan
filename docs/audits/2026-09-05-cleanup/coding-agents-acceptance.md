# Coding Agents acceptance — September 6

**Implementation is qualified; full operational acceptance is not achieved.** The final candidate passes full lint/type/test validation, UI/web builds, Electron packaging/native smoke and unchanged web bundle limits. Fresh direct responsive checks and matched typing measurements pass. The manual native regression is verified with separately reviewed grader failures. Natural verification stopped after one automatic boundary in each of two attempts; its second-boundary requirement remains unverified. OpenAI core interaction coverage is partial after a repeated Plan-card visibility failure; Anthropic and xAI access is blocked in the current private profile.

## Qualified implementation

The final integration fixes authoritative Plan restoration across delayed parts, reload, session switches and draft promotion. Retrieval is bounded, missing authority defers restoration and uncaptured sends, and later canonical parts can refine inferred state. Explicit user choices and already captured queued selections remain authoritative. Only exact automatic maintenance messages receive special handling; ordinary synthetic records retain their existing behavior. Model/effort restoration remains separate from Plan selection.

Managed automatic continuation carries the complete applicable Plan instruction. Managed execution uses its actual parent's authority; later approval cannot retroactively authorize an earlier invocation. Missing authority fails explicitly. Explicit Implement Plan authorizes the current revision and transitions to implementation. Exact managed maintenance output cannot create a new actionable plan, replace a saved revision or trigger plan-ready host notifications. Native compaction continuation retains the existing Plan-card behavior, which was observed in the natural attempt; this is not evidence of blanket suppression of all automatic output. Primary-agent planning remains instruction-based; this work does not add a new primary permission boundary.

Focused integration covers 517 UI tests, 147 Plan/backend tests, 212 related core tests, 74 notification/presentation tests and 44 performance-helper tests. These focused counts overlap the complete gate and must not be summed as unique coverage. `bun run validate:full` passed on settled source, including repository lint, package type checks and the full test command. Shared web/Electron contract tests remain included; VS Code runtime QA is excluded.

Existing settings, saved-plan formats and compatibility identifiers remain intact. No persistent memory store, database migration or new project dependency was added. Earlier fixes to Default/explicit effort capture, reasoning presentation, optimistic labels, scroll anchors, mobile controls, managed adapters, Spark summary compatibility and package-manager discovery remain part of the candidate. Their original reproductions and scopes are preserved in [historical evidence](coding-agents-history.md) and the [findings register](findings.md).

## Candidate identity

| Item | Qualified value |
|---|---|
| Source | `3c96c656f19a3026612ab7313034d88de070c8e82b1876bcd0c91fcaa3656426` |
| Scripts at full gate, manual and performance runs | `5c448f7251a97b87db70d7c890067e8dd0e6494a1430e5b92665365ca4589367` |
| Scripts after one natural QA prompt clarification | `6a32105219487d537fca621b992fe4fe40f0f87b8d2574d6b6cedd5124d730c0`; 40 related QA tests and changed-file lint pass; product/build unchanged |
| Scripts after one core response-reveal correction | `890cf65db94235bf8b84ed3dbcda8ea25118e268a262c814ac804b2e14f5db19`; 16 related QA tests and changed-file lint pass; product/build unchanged |
| Served UI | `.cache/qa/candidate-web-OVdoR6`, SHA-256 `7cd8ff581fdcdeb6e828e13ef6a07988ac1cf2efc3282803719a41990ecccf85` |
| Packaged Electron | `.cache/qa/packaged-electron-YJDmZ4/package-evidence.json` |
| Package archive | `0f0ebea2377f41acc4f782a91ae6bc7d39202f059c184e355901cbfbf3b151bf` |
| Native runtime | Isolated OpenCode 1.18.29 |
| Web startup graph | 4,660,536 raw / 1,365,540 gzip bytes; unchanged limits pass |

The actual archive's 31 default-config files and 16 plugins pass packaged-default checks. SQLite and PTY native smoke checks pass. This is a local unsigned QA package; signing, updater installation, publication and legacy Tauri migration are outside scope. `.cache/audit/coding-agents-2026-09-05/finish-qualification-r1/` retains the full gate, builds, package evidence and bundle receipts, including an initial post-packaging inspection lookup failure followed by a successful smoke-only correction without rebuilding.

## Fresh verification

| Requirement | Outcome and evidence |
|---|---|
| OpenAI live core interactions | **Partial: 12 of 16 effort settings verified, one failed, three unrun.** All eight web settings and four Electron Plan-off settings pass send, reload, real cancel, reconnect and duplicate checks. Electron Builder Plan-on Default repeats the Plan-card visibility failure after the one scoped correction; remaining Electron Plan-on settings are stopped. This does not establish full coding/manual cells. |
| Electron/web core fixtures | **Partial / unverified as complete journeys.** Each fresh run passed 22 checks, then failed before the fresh Plan send because the saved-Plan fixture caller supplied the obsolete helper contract. Original failures remain unchanged. The repeated QA class was stopped without further harness expansion. |
| Core behavior observed before that stop | Queue capture, attachments, rejection rollback, permission/question UI, empty/delayed reasoning, tool failure/recovery, cancellation/reconnect, history anchoring, Default/High restoration and mounted/virtualized original Plan approval. Both sets of 28 screenshots are reviewed; 1,431 journal records per run have no gaps. Images named for a disabled superseded Plan actually show the transcript and do not establish that button's visual state. |
| Responsive web | **Verified for direct fixture-backed controls.** Nine checks pass: Default/High submission and reload, unsent Low through responsive/session round trips, then rich states at 390×844, 844×390 and 768×1024 in both themes. All 91 original screenshots reviewed; 165 journal records have no gaps/errors; console and unknown-route lists are empty; 62 retained process identities are absent. Landscape captures show individual revealed targets, not the whole Plan document simultaneously. |
| Typing during background streaming | **Verified measurement.** Three fresh runs per package, 360 trusted inputs total; no draft submission or streaming-progress loss. Median p50 32.5→32.8 ms (+0.92%); p95 38.5→38.9 ms (+1.04%). Ranges overlap: no clear change. |
| Session memory | **Unverified.** Corrected baseline stopped at canonical/rendered pagination coverage after the initial checkpoint. Loaded/inactive/deleted windows were not reached; five arms remain unrun. No retention or leak conclusion. |
| Other interaction timings | **Unverified where prior evidence is insufficient.** The existing full interaction protocol repeatedly stopped at pagination. Correctness fixtures and diagnostic overrides do not substitute for a matched timing comparison. |
| Native manual representative | **Native continuity verified; original automated run failed.** Sol/Orchestrator/Plan-on High retry `7AkpDR` passed both native boundaries, Plan-on steering after reload, revision-2 preservation, fresh current-plan approval, actual failing-test → edits → passing-test work and independent implementation tests. Five planning children carry enforced read-only tools. The strict disposition grader counted eight calls because one successful ninth result has appended diagnostic prose. Independent call/envelope/timestamp correlation verifies all nine acknowledged exactly once with no active or pending barrier. |
| Generated application's browser review | **Partial.** A SHA-verified copy of the manual result passed priority creation/persistence, creation order and filtering with unchanged summary. All three screenshots independently reviewed show readable labels and red/amber/blue markers. The exact-RGB helper failed on pseudo-element markers using slightly different shades; its mobile keyboard step was not reached. The failed direct run remains unchanged. |
| Native natural representative | **First boundary verified; full journey failed / second boundary unverified.** `Q2L8NQ` observed native automatic compaction at 257,903 tokens against the unchanged 256,000 threshold, with an active read-only child bracketed across native start and summary exit. The one focused retry `rnFZtg` repeated that proof at 257,706 tokens and passed the corrected second-witness marker/read-only/completed-awaiting prerequisite. It then timed out waiting for idle Send while actual managed recovery was busy collecting the result. No second boundary or implementation occurred; this objective is stopped without further harness changes. |
| Native attachments/dialog/quit | Earlier Oi3VJf chooser, attachment payload and quit results remain scoped evidence for their unchanged native implementation. They are not a fresh final-candidate live-compaction pass. |
| Spark Explorer | Earlier corrected Spark High/Sol workflow verifies exact request options and managed reconciliation; focused coverage only. |
| Physical-device keyboard | **Unverified.** Desktop Chromium emulation does not establish device keyboard behavior. |

Fresh fixture evidence is under `.cache/qa/finish-r4-MtRhNn/fixtures-evidence/`. Direct mobile evidence is `.cache/qa/adapter-probes/mobile-rich-Ga36Qc/{result,root-review}.json`, with outer ownership/provenance in `.cache/qa/finish-r4-MtRhNn/mobile-closure.json`. Its journal is preserved and the private runtime has been removed. Performance protocols, comparisons and all failed attempts are detailed in [performance](performance.md).

Manual retry evidence is under `.cache/qa/finish-r4-MtRhNn/manual-network-retry-evidence/`, ending `7AkpDR`. `root-closure-review.json` correlates all nine actual `continue` calls against distinct final envelopes and acknowledgment timestamps; automatic collection is not needed to explain the discrepancy. The omitted call is `call_7jFtAJ3h84LufQTVoepXFzbt`, child `ses_f8b0956bbffe5hizujnCwLEfVF`. The journal contains 5,494 records and no gaps; console and cleanup errors are empty; all eight images are reviewed, 127 retained process identities are absent, and all 16 original/archive file hashes match. The original first attempt `JpEeas` failed `ERR_NETWORK_CHANGED` before any prompt and remains preserved. The supplemental project review is `direct-project-evidence/manual-result-55GGIZ`; all eight owned process identities are absent. No grader or product code was changed for these supplemental inspections.

A subsequent direct in-app browser check of that same generated-project copy observed keyboard focus on the priority select, with a visible outline and no document overflow. The requested 390×844 override produced an effective 433 CSS-pixel width; some native control captions appear clipped in its screenshot. `direct-browser-mobile-review.json` therefore records partial evidence, not a pass at the requested size. The tab closed, the viewport override reset and the exact temporary server identity was verified absent. This limitation concerns the generated project; it does not replace or invalidate the separately verified DevRyan responsive fixture.

Natural attempt `Q2L8NQ` retains 2,612 gap-free journal records, all five reviewed PNGs, 92 absent process identities and 14 verified original/archive project files. Console and cleanup errors are empty. `phase-marker-correlations.json` and the final ledger correlate dispatch `call_WIc2wjzOyW2ckrpStW0Ys65Y` with child `ses_f8ae95d95ffeJj8uNNpCI8EBHh`: the child prompt lacks the marker while the rendered label and parent reply identify it. The first-boundary native continuation also generated a Plan card; `first-boundary-presentation-correlations.json` records its canonical parent linkage. The retry is configured as `natural-marker-retry.json`; `natural-marker-qualification.json` records the isolated helper change and unaffected application identity. No second boundary or completed natural implementation is credited from the failed attempt.

Retry `rnFZtg` retains 3,216 gap-free records, five individually reviewed images, 96 absent process identities and 14 original/archive file hashes. Its console and cleanup errors are empty. `second-boundary-send-correlations.json` links the visible busy/Stop state to managed recovery user `msg_0752b0c08000680cb4e9d4e3c9` and assistant `msg_0752b0c54001a4ooq91CmegQUq`. The complete Plan instruction accompanies the automatic recovery marker. The next audit batch stayed in the composer while the helper waited for Send. Automatic collection was neither disabled nor delayed for the test, and no completed child was recreated. The stopping rule applies to the uncompleted natural objective; independent core interaction checks were continued separately.

The first web/Builder/Plan-on/Default core attempt `IHcLFZ` failed the visible reconnect marker check because the complete response was a collapsed Plan card. Its exact final marker exists in canonical assistant `msg_07534774c0018uMZx7RC7gPYew`; the following reload/duplicate checks were not reached. All three images are reviewed, 273 journal records have no gaps, 35 process identities are absent, and 14 archived project files match. One focused core correction now uses that exact response's Expand Plan control when present and the existing text-reveal helper before the same strict visibility checks. Retry `cL5yOw` passes all nine checks with clean closure, but produced ordinary visible text; it does not alone demonstrate the conditional expansion path. `core-reveal-qualification.json` records the isolated change. Raw failed attempts remain part of the coverage table.

Electron/Builder/Plan-on/Default `PhDlDB` repeats that visibility failure with the corrected helper. Canonical assistant `msg_0754a89c1001sDHPJ4mAQ5sY8U` completed with a Plan-formatted response containing the exact reconnect marker; the failure screenshot shows its collapsed Plan preview. The first eight checks pass, but visible completion, the subsequent reload and duplicate assertion remain unverified. All three PNGs are reviewed, 277 journal records have no gaps, 57 process identities are absent, and all 14 original/archive/seed file hashes match. Console and cleanup errors are empty. No second helper change or retry was made. Remaining Electron Plan-on core settings are unrun under the same stopping rule; passing ordinary-text responses elsewhere do not qualify Plan expansion.

## Required live coverage

The approved scope is 24 model × primary-role × Plan × runtime cells, each covering Default and supported explicit effort, plus 12 Electron natural-compaction combinations. Builder and Orchestrator, Plan on/off, Electron and web remain in scope; responsive checks supplement web. Compatible journey observations may be combined, but no incomplete cell counts as passed.

The [coverage table in JSON](coding-agents-coverage.json) enumerates all 24 cells and 12 natural combinations, with separate Default/High interaction outcomes and reviewed raw-run references. A passing core interaction does not imply manual-compaction or coding-work acceptance for that cell.

OpenAI's 16 required core effort settings finish at **12 verified, one failed and three unrun**. Across all providers, the other 32 effort settings are blocked. The 14 raw OpenAI core attempts include both retained visibility failures and the separate successful web retry. Their closure receipt, `.cache/qa/finish-r4-MtRhNn/core-closure.json`, verifies all 42 original screenshots were reviewed, 3,707 journal records are gap-free, 196 archived files match their seeds, 642 distinct retained process identities are absent, and no private core runtime remains. Final product, scripts and served UI hashes match the qualified identities above.

| Provider/model | Eight live cells | Four Electron natural combinations | Current access |
|---|---|---|---|
| OpenAI / Sol 5.6 | **Partial; no fully accepted cell** | **Unverified**; one boundary in each representative attempt | Copied access passes scenario-duration admission |
| Anthropic / Sonnet 4.6 | **Blocked** | **Blocked** | Fresh profile reports no supported unexpired access |
| xAI / Grok 4.6 | **Blocked** | **Blocked** | Fresh profile reports expired/near-expiry access |

No credentials were refreshed. The fresh actual tool inventory must contain `devryan_task` before the first provider prompt. Manual and natural representatives must each reach two native boundaries, preserve steering through reload, approve the current saved revision, implement real work, pass independent tests/browser inspection and reconcile managed results exactly once before broad matrix acceptance. Planning file preservation and actual child read-only enforcement remain live requirements; passing unit tests do not replace them.

The representative gate did not pass completely. Seven prepared broad project journeys, four manual journeys and three other natural journeys remain unrun; the failed natural objective was not reopened through another cell. Core interactions were independently useful and proceeded until their own Plan-card visibility stopping rule applied. This is a closed verification attempt with explicit gaps, not a full acceptance pass or a claim that the remaining matrix was executed.

## Delivery and limits

Final cleanup audit `.cache/qa/finish-r4-MtRhNn/final-process-audit.json` covers all 20 fresh full-fixture, manual, natural and core attempts: 1,020 distinct recorded process identities are absent, cleanup-error lists are empty, and no private runtime remains. Failed project originals and all archived evidence are retained. Direct mobile, generated-project browser and performance runs retain their separately reviewed closure receipts.

[Memory decision](memory-decision.md) records why no persistent memory was added. [Fresh-checkout QA instructions](../../QA.md) and the [reusable cleanup procedure](../reusable-cleanup-checklist.md) describe reproducible preparation, evidence capture and owned-process closure. Earlier resource/backend results retain their artifact and workload limits; the CPU/GPU reduction targets remain failed. Bots, VS Code runtime QA, releases, other repositories and personal configuration changes remain excluded.
