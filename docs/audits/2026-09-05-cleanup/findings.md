# Findings register — DevRyan cleanup pass

Scope: repository-owned code, scripts, instructions and shipped defaults. Web/Electron/mobile acceptance only; VS Code runtime work was removed at the user's request. Package inventory is a routing and representative contract review, not a claim that every source line was inspected. Personal skills, other repositories, releases, and production changes are excluded.

| ID / location | Evidence and impact | Change / verification | Disposition |
|---|---|---|---|
| F01 `ui/src/sync/event-reducer.ts` | Part updates repeatedly scan the same history through role/streaming wrappers; cost grows with history length. | Reuse one owning-message lookup while preserving exact-session recovery and unscoped compatibility semantics. 75 reducer tests; three-process before/after measurements in `performance.md`. | **Fix** implemented; reducer-only performance evidence. Whole-app latency remains unmeasured. |
| F02 `ui/src/sync/session-selectors.ts` and `.test.ts` | Exported reference comparator has no production caller except its own subscription helper. Tests directly assert the comparator and manually increment a fake render counter, rather than exercise a store. | Inline the reference comparison. Replace two shallow tests with one real Zustand subscription test: 60 unrelated updates notify zero times, a session update notifies once, unsubscribe stops delivery. Temporary `if (true)` fault made the replacement fail; restored source afterward. Existing two leaf-selector tests retained. | **Fix**. Both removed test behaviors have stronger equivalent coverage; no deletion quota. |
| F03 `ui/src/lib/i18n/messages/controlCopy.test.ts` | Two policy scans retain every parsed UI AST between assertions. A full affected gate hit a 9.98 s / 5 s timeout under load. | Scan each parsed file for both policies, retain only findings. Six isolated assertions pass in each of three runs; peak RSS median −47.9%. Elapsed time was worse, so no speed claim. Full-gate result belongs in `README.md`. | **Fix** memory use; **retain** the title-case policy checks. |
| F04 `ui/src/components/layout/MainLayout.tsx` | Drawer width was measured only when `isMobile` changed. Resizing while mobile retained old offsets; earlier reviewed captures showed a closed drawer strip. | Width now follows screenWidth; layout effects place resized drawers at their endpoint before paint. Six resized theme/viewport captures reviewed; closed strip absent and composer within viewport. Touch open/close verified at 390 and 768 widths in both themes; physical keyboard/device acceptance incomplete. | **Fix**, with limited visual verification. |
| F05 `scripts/perf/electron-resource-benchmark.mjs`, `scripts/qa/` | CDP inspection, fixture and process ownership existed separately from repeatable host QA; socket closure left requests waiting for timeout. | Share CDP/port helpers, reject pending commands on disconnect, reuse existing owned-group shutdown, add isolated web/Electron launcher and explicit live HTTP smoke. Nine focused Node tests passed; final Electron/web journeys pass with reviewed screenshots and explicit coverage limits. | **Fix** debugging access; **defer** remaining journey coverage described in F15. |
| F06 `AGENTS.md` | Stack claimed Node >=20 and installed Radix/HeroUI dependencies contrary to manifests. | Correct Node minimum and remove obsolete dependency guidance. Compatibility names, security rules and Tauri cutover policy unchanged. | **Fix** factual instruction drift. |
| F07 `web/server/lib/opencode/DOCUMENTATION.md` | Public domain re-export boundary was described as a baseline placeholder. | Correct the module description; callers still use the same boundary. | **Fix** documentation; **retain** the re-export module because it defines a real integration boundary. |
| F08 UI source-text tests (`DesktopBrowserPane`, `ManagedTaskList`, `SessionNodeItem`, Bot UI, mobile layout) | Some large tests have dozens of regex assertions. Counts identify review candidates, not redundancy or safety. Existing real policy, native hit-testing and lifecycle tests also guard contracts. | Reviewed the Coding Agents assertion groups in `source-test-review.md`. Remove three duplicated card-border source checks already enforced by the real five-row ManagedTaskList render test; retain caller-wiring, lifecycle and distinct layout protection. All 51 ManagedTaskList tests pass. | **Fix/retain** after scoped review. Bots test conversion excluded; no broad deletion. |
| F09 packaged defaults and owned agents | Canonical orchestrator prompt is ~34.6 KB; legacy Tauri copy ~31 KB. Project planning instructions contain old absolute path examples. Prompts and tool contracts are packaging inputs, not ordinary dead text. | Canonical asset inventory contains no machine-home paths; repository-local personal configuration is not a packaged input. Retain role, plan, task, recovery and Tauri compatibility contracts. See `packaged-instruction-review.md`. Three fresh compacted/control pairs did not reproduce unresolved missing-plan inference; every compacted arm read its exact saved plan. The final compacted arm remains failed for an unanswered approval question. | **Retain** after scoped packaging and matched diagnostic review; this symptom does not justify retrieval/memory additions. General operational compaction acceptance remains separate. |
| F10 owned `agent-browser/assets/agent-browser/SKILL.md` | Repository-owned skill (~4.5 KB) is an installed browser tool contract; personal skills are out of scope. | Retain packaged skill and its asset ownership. No personal skill edits. | **Retain** with tool-contract rationale. |
| F11 direct dependencies and dynamic adapters | Workspace manifests, lazy views, platform adapters, optional native modules and public barrels make grep-only “unused” conclusions unreliable. No proven unused direct dependency found in this pass. | Preserve lockfile/dependency identities. Concurrent SDK/version changes are another task's work, not audit removals. | **Retain**; **defer** exhaustive dependency reachability analysis. |
| F12 startup bundle graph | Built web graph exceeds existing raw/gzip limits. Build overlapped unrelated SDK/version edits, so clean before/after attribution is unavailable. | Trace six small markup utilities into eager Markdown; separate their chunk without changing budgets. Three fresh matched builds per condition reduce startup JS by 402,849 raw / 120,374 gzip bytes. See `startup-bundle.md`; later candidate sizes are in `coding-agents-acceptance.md`. | **Fix** startup bytes; current web limits pass. Whole-app measurements remain separate. |
| F13 deterministic QA fixture and readiness | Earlier runs duplicated session identities across directories, returned `all` instead of `providers` for config, and used session IDs rejected by recovery safeguards. Reload also initially read the old DOM before navigation committed. | Scope fixture responses by directory; return each endpoint's contract; use valid session IDs; seed the browser's isolated workspace preference before mount; wait for load before restored-content checks. Final Electron and web stream/send/cancel/reconnect flows pass, with complete numbered response text and distinct DOM user IDs verified. | **Fix** fixture/runner fidelity. Original failures retained in ignored evidence; no production safeguard relaxed. |
| F14 mobile fixture controls and landscape row actions | Model control initially showed “Not selected”; fixed by F13. At 844×390, a settled long session title overlapped the fully visible Pin action by 2px in both themes. | Increase only session-row trailing padding. Before/after screenshots were opened: the rebuilt candidate has a 10px title-to-Pin gap in both themes; the original 20×20 Pin/Archive pointer targets remain visible and hittable. Exact captures and geometry are recorded in `visual-review.md`. Earlier six-layout composer/drawer checks remain scoped evidence. | **Fix** fixture model fidelity and sidebar title/action overlap. Rich responsive fixture acceptance is in progress; physical-device keyboard and captured session-change evidence remain separate. |
| F15 full acceptance gaps | Approved coverage requires 24 live cells with Default/explicit effort, manual continuity throughout, 12 Electron natural combinations, responsive/native interactions and three-run matched measurements. | Fresh full qualification, direct mobile, typing and manual native continuity have reviewed evidence. The natural representative stopped after repeated QA failures; other provider access is blocked. The coverage JSON distinguishes core interactions from full-cell acceptance and preserves failed attempts. | **Not fully accepted**; remaining compaction/coding cells and memory/interaction measurements are explicitly unverified or blocked. |
| F16 `scripts/agent-evals/client.test.mjs` | Cycle/cap test's 100 ms deadline expired under concurrent work although it tests graph integrity, not time. Isolated rerun passed. | Give only the graph test 1 s scheduling headroom; preserve exact reason/order assertions and the separate 40 ms hung-discovery deadline test. Full gate passes afterward. | **Fix** flaky test timing, with no production timeout change. |
| F17 `ui/src/components/chat/MobileAgentButton.tsx` | One trusted touch opened the picker on pointerup; its synthesized click then hit Orchestrator inside the new panel, changing Builder/Low to Orchestrator/Default without a second gesture. | Open on click using adjacent model-button precedent and preserve composer focus on pointer down. Exact before/after native event traces and reviewed agent-sheet screenshots are in `visual-review.md`; 16 focused tests and file lint pass. | **Fix**. After-build tap preserves Builder/Low, opens the full picker, accepts keyboard focus and closes on Escape. Physical-device keyboard behavior remains separate. |
| F18 `ui/src/components/chat/ModelControls.tsx` | The mobile inline effort list omitted Default. Its separate overflow picker had Default but was unreachable for short provider lists such as fixture Low/High, so explicit effort could not be cleared through that surface. | Add Default before generic inline choices, reusing the existing null-selection path. Rebuilt-candidate native mobile High then explicit-empty Default submissions and Default restoration after reload pass twice, before any managed-task read fixture; screenshots were reviewed. | **Fix**, with scoped actual-control/canonical/reload evidence. The encompassing mobile diagnostic still fails at a separate landscape reveal step; final runner acceptance remains open. Cursor-specific behavior is unchanged. |
| F19 `ui/src/components/chat/ModelControls.tsx` and session selection ownership | Resizing after selecting unsent Low remounts `ModelControls`; returning A→B→A at constant viewport also revisits the local restoration guard. Both reapply A's latest canonical Default over its newer unsent choice. Exact before/after screenshots, DOM samples and separate native sends reproduce each path. | `mobile-rich-yT2dPj/responsive-selection-review.json` and `mobile-rich-6wPOMf/session-selection-review.json` each record expected `low`, actual raw/canonical empty variant, matched journal and zero gaps. The per-session canonical restoration identity is integrated with the selection lifecycle; the rebuilt 4ZmpkJ/pHrgLt candidate passes 48 focused tests, lint and UI/web builds. | **Fix verified** in bounded H79wTt: both actual Low submissions retain low through raw transport, canonical records and journal; 73 PNGs reviewed. Full matrix acceptance remains separate. |
| F20 touch sidebar action geometry | At 844×390 with a coarse pointer, the global 36px touch minimum expands an invisible context-menu anchor and shifts Pin/Archive into the title. The leading child toggle also covers the first title glyph; its actual glyph center hits Collapse Subsessions. | Exact DOM rectangles, glyph ranges, hit tests and reviewed PNGs are retained in `mobile-rich-Stxv56` and `mobile-rich-hUzxay`. A narrow correction keeps the invisible anchor at zero size and reserves trailing/leading clearance only in the non-drawer touch layout. | **Fix verified** in bounded H79wTt: both themes retain 10px title/action gap, correct 36/20px targets and title-owned glyph hits. Full matrix acceptance remains separate. |
| F21 QA process cleanup | The six-arm study reported successful cleanup while an independent OS audit found 30 surviving language-server descendants across its exact private profile paths. | Preserve the original reports; six exact owned native parents and 30 descendants are now closed, with an empty final PID audit and retained residual files. Correct cleanup verification before the full matrix. | **Fix implemented** with retained ancestry/start identities, guarded termination, final OS audit before profile removal, and meaningful detached-child tests. All 408 script tests pass; pHrgLt native quit confirms zero survivors. Seven old native roots/42 total processes were closed across precisely scoped audits; original reports remain unchanged. |
| F22 coarse workspace heading in `sidebar/sortableItems.tsx` | Global 36px touch targets outgrow the 24px spacing assumptions: title c/e glyphs hit New Worktree, and Menu/New Draft boxes overlap by 10px at 844×390. | Separate zero-send native glyph/rect proof and reviewed screenshot: `mobile-rich-yBRU7s/workspace-heading-glyph-review.json`. Actual 390×844 and 768×1024 drawers also overlap Menu/New Draft by 12.61 px in `mobile-rich-dzChzJ`. The action-count/offset correction is integrated in dOSUpt/cxsg0V and passes lint/build/native packaging smoke. | **Fix verified** in bounded zero-Send odFHOw: three coarse sizes, title glyph ownership, 36px targets with 3.59–4px gaps, and actual menu open/close; all nine PNGs reviewed. Full rich active-session matrix remains separate. |

The expanded Coding Agents work also produced the findings below. Their detailed reproductions, candidate identities and retained failed attempts are in [Coding Agents acceptance](coding-agents-acceptance.md). The final-candidate full validation gate passes on source `3c96c656…`; [current acceptance](coding-agents-acceptance.md) separates fresh, historical and incomplete coverage.

| ID / location | Evidence and impact | Change / verification | Disposition |
|---|---|---|---|
| F23 model effort selection and canonical restoration | Missing, explicit Default and explicit effort were not preserved consistently through every send/history path. A live manual summary also restored Medium over the user's Default. | Preserve the three intentions through configuration, queues and native serialization; restore from the latest actual user's canonical model choice, including the typed native empty-string Default. Ignore native summary/continuation users for restoration. Synthetic Default/High boundaries and live manual preflights verify selection preservation. | **Fix implemented**; final provider matrix remains open. Native adapter defaults are reported independently from UI intent. |
| F24 `ReasoningGroup.tsx` / `ReasoningPart.tsx` | Active or completed reasoning without exposed text could leave blank expandable controls. Lazy Markdown could also delay available text. | Render a non-expandable active status, omit completed empty content, and show available text while Markdown loads. Focused real-component checks and ten reviewed screenshots pass; final rich fixtures cover empty/delayed states. | **Fix verified** for exposed UI content. No claim to hidden provider reasoning. |
| F25 `sync/session-plan-selection.ts` and Plan restoration | A gap-free live Orchestrator journal showed Plan enabled on three human turns but missing after reload. | Restore authoritative Plan choice while preserving newer unsent intent and Implement Plan's off action. Recognize exact native maintenance records while preserving ordinary synthetic behavior. Focused regressions and fixture reload/approval checks pass; F35 records the later delayed-authority correction and fresh manual proof. | **Fix implemented; fresh two-boundary manual regression verified.** Natural and broader matrix acceptance remain unverified. Superseded plan cards remain disabled. |
| F26 optimistic user messages | Actual first-send controls briefly changed Low → Default → Low before canonical echo. Both observed queued requests still retained Low, so a wrong queued transport was not demonstrated. | Include the captured variant in optimistic normal/slash user rows. Forty delayed-echo samples, queue capture, canonical restoration and second dispatch retain Low on the corrected build. | **Fix verified** for the reproduced label transition; avoid attributing an unobserved queue failure. |
| F27 `useConfigStore.ts` catalog completion | Late catalog refresh replaced a draft's visible role/model with defaults while Send retained its original captured selection. | Reuse the authoritative current-draft restoration resolver when the catalog completes. Before/after actual controls and submissions, six reviewed screenshots, and three asynchronous regressions verify consistent selection. | **Fix verified** for the refresh race. |
| F28 message history and scroll anchors | Packaged history pagination shifted the final page by 40px. Tracing showed a successful correction applied against a user row clamped by a sticky ancestor. | Use the normal-flow owning turn for user-message anchors and preserve exact assistant geometry. Corrected packaged pagination stays within the unchanged 2px gate. Later 148-action diagnostic used an explicit runner override and is not final timing evidence. | **Fix implemented**; the original-runner full candidate workload and repeated performance runs remain required. |
| F29 saved plan paths | A long fixture project ID exceeded the filesystem component limit and caused `ENAMETOOLONG` in the plan-revision API. | Hash only overlong project-ID storage components, preserving existing valid paths and public IDs. Shared web/Electron contract checks pass. | **Fix implemented**; no compatibility-ID migration. |
| F30 managed Slim and Meridian integration | Installed Slim exported a server descriptor the wrapper did not accept. Separately, actual Anthropic cancellation did not propagate through the managed Meridian HTTP adapter. | Accept the verified descriptor export; apply a narrowly fingerprinted managed Meridian native-HTTP patch preserving disconnect cancellation. Wrapper regressions and an actual Anthropic send/cancel/reconnect journey pass. | **Fix implemented**. Personal packages and legacy Tauri assets remain unchanged; final provider matrix is separate. |
| F31 automatic package-manager discovery | Native Node profiling attributed host pauses to synchronous probes during automatic update checks; handlers were fast after delayed dispatch. | Bounded asynchronous probes retain precedence, caching and the synchronous CLI contract. Fresh AB/BA/AB measurements and 76 independent checks pass: median cold request 10,151.37→8,052.16ms (−20.68%), median maximum health delay 10,137.94→6.40ms; 1,424 unique fingerprints identify only the intended module difference. All journals are gap-free and recorded processes absent. | **Fix verified** for local fixture discovery, three observations per version. This is not renderer CPU or whole-app acceptance. Earlier provenance failure remains retained. |
| F32 delayed historical parts and unsent effort | A rendered real-control test starts with historical High metadata and missing parts; selecting Low before parts arrive restored High over the newer choice. The untouched control correctly restores High. | Session-owned manual intent survives delayed parts, older queued sends, failures and session switches, and is consumed only by its matching successful send. Eight mounted regressions and the subsequent complete gate pass. | **Fix implemented and behaviorally verified**; final live matrix remains separate. |
| F33 OpenAI Spark summary compatibility | A real High Explorer child on `gpt-5.3-codex-spark` failed with API400 `unsupported_parameter` for `reasoning.summary`, before child output/tool activity. The native catalog and final options advertised/sent detailed summary. | The shared plugin omits summary for exact Spark/API targets and its existing Fast alias at catalog and final request hooks, preserving effort and unrelated options. Six new regressions fail before the change; all 22 plugin tests pass after it. The corrected Node-launched pair `spark-explorer-pair-tjaZyz` passes every inventory, model/High, evaluator and managed-result check. Independent review verifies five Spark native requests with High and no summary, gap-free journals and all 44 processes absent. Earlier failed attempts and the separately reproduced Bun test-loader problem remain retained. | **Fix verified** in one focused Spark/Sol workflow, not broad model-quality or UI acceptance. Full validation, UI/web builds and fresh Electron packaging pass; the intervening scheduler correction only synchronizes its test with terminal persistence. |
| F34 shared assistant image extraction | Candidate CPU samples identify AssistantTextPart → image extraction → code masking even when streamed text contains no opening bracket. The complete parser can emit a reference only through a literal `[` branch. | Return the existing empty result before masking when that necessary character is absent. Supported Markdown/reference destinations and standalone canonicalizers remain intact; 14 focused tests pass. Three fresh processes per version show separated function-cost ranges for four fixed no-image inputs; exact results are in `performance.md`. | **Small optimization implemented and fully qualified** on the final candidate. This removes impossible scans; it does not establish the four-stream CPU target or justify replacing Markdown parsing. |
| F35 Plan authority, restoration and automatic continuation | The native HaFl6X run preserved saved revisions but submitted the human steering after compaction/reload with Plan off. Incomplete canonical parts and maintenance continuations could also incorrectly refine restoration or create actionable presentation. | Bounded authoritative retrieval, missing-parts deferral, selected-session ownership and explicit-user intent preservation; exact maintenance recognition; complete continuation Plan policy; actual-parent managed authority; non-retroactive approval; explicit Implement Plan transition; maintenance-safe cards/revisions/notifications. Focused regressions and the final full gate pass. Manual retry `7AkpDR` verifies Plan-on human steering after reload between two native boundaries, unchanged revision 2, actual current-plan approval and independently passing implementation. | **Implemented; regression verified in the fresh manual native journey.** Broader matrix/natural coverage remains separately graded. No persistent-memory loss or new primary-agent permission boundary is claimed. |
| F36 Missing history and captured send ownership | Reloaded sessions could capture a new send before canonical Plan authority became available; applying that guard to already captured queues would wrongly strand valid queued work. | Bounded selected-session materialization and loader recovery; guard new uncaptured sends while allowing captured direct/queued selections through unchanged. Mounted regressions reproduce and fix the queue regression; full validation passes. | **Implemented and test-verified**. No new broad high-frequency store or heuristic history-based live state. |
| F37 Performance and fixture prerequisites | Old typing navigation, scroll-height pagination and saved-Plan helper contracts prevented completion of acceptance protocols. | Approved typing/pagination corrections pass 44 focused tests. Six fresh typing arms pass with overlapping ranges. Memory pagination fails again before loaded/inactive/deleted windows; both full fixtures stop at the stale saved-Plan caller. Direct existing mobile coverage passes nine checks/91 reviewed PNGs on the final candidate. | **Typing/direct mobile verified; full fixture, memory and remaining interaction timings unverified.** Preserve failed runs and apply the stopping rule instead of further harness expansion. |
| F38 Live Plan-card visibility helper | Web `IHcLFZ` and later Electron `PhDlDB` contain the exact completed reconnect marker in canonical Plan-formatted responses, but the helper cannot establish visible completion in their collapsed cards. | One focused response-reveal correction passes related tests. The separate web retry passes with ordinary text; Electron repeats the visibility failure afterward. Both failures have reviewed screenshots, gap-free journals, unchanged project files and verified cleanup. | **Failed QA objective; remaining Electron Plan-on core settings unrun.** No further helper expansion. Passing ordinary-text core responses do not establish the conditional Plan expansion path. |

## Package dispositions

Paths below are under `packages/`. The counts in `inventory.json` are tracked-file/test-file inventory counts; Tauri's Rust tests do not appear in the JS test count. Retain means no justified deletion was selected from the representative contracts reviewed. It does not mean a package is free of cleanup opportunities.

| Package | Reviewed responsibility / evidence | Disposition |
|---|---|---|
| `ui` | Sync reducer, leaf subscriptions, layout, title-case audit; source-heavy UI test candidates and lazy view boundaries. | **Fix** F01–04; **retain** scoped source-test contracts after F08 review; full interaction traces remain in progress. |
| `web` | Server composition, diagnostics/auth routes, OpenCode adapters, default-config manifests, CLI mode policy. | **Fix** F07; **retain** authenticated diagnostics, CLI policy gates and dynamic adapters. **Fix** startup bundle F12; remaining journey acceptance is in progress. |
| `electron` | In-process server/native preload architecture, origin/quit cleanup and real native pointer checks. | **Retain** native boundaries and regression coverage. Applicable packaged attachment/dialog/quit checks pass on unchanged executable/UI bytes; original identities and scope remain explicit. Final live scenarios and repeated package measurements remain open. |
| `desktop` | Legacy Rust host, sidecar and updater compatibility; manifest-owned defaults. | **Retain** released compatibility; no new Tauri features or asset deletion. |
| `vscode` | Shared dependency boundary in workspace inventory. | **Defer/excluded** runtime audit and QA per user steering; ordinary full-suite compatibility checks retained. |
| `orchestration-runtime` | Admission, dispatch barriers, auto-resume and ownership policies. | **Retain** behavior and policy tests; no evidence for safe wrapper removal. |
| `harness-runtime` | Journal sanitizer/retention/gaps, lifecycle correlation, atomic private persistence and command deadlines. | **Retain** bounded instrumentation and durability tests; reuse sanitizer in QA output. |
| `shared-runtime` | Safe archive, image Markdown parsing, configuration apply and shared normalization. | **Retain** dependency-light cross-host contracts and malformed-input tests. |
| `cursor-sdk-runtime` | Runtime adapters, recovery, active-entry-protected cache with cap and TTL. | **Retain** adapter boundaries and lifecycle tests; do not remove provider fallback by static call search. |
| `bots-runtime` | Strict JSON, exact scopes, leases and policy transitions. | **Retain** security/correctness assertions. |
| `bot-supervisor` | Fixed-verb lifecycle authority and ownership. | **Retain** isolation and admission coverage. |
| `bot-engine-proxy` | Sole Docker socket owner with fixed operation surface. | **Retain** proxy boundary and operation denial checks. |
| `bot-egress` | Model/browser purpose scopes, exact bearer checks, metadata denial. | **Retain** egress policy tests; no relaxations for QA. |
| `bot-computer` | Reviewed command execution and persistent scoped browser. | **Retain** scoped command/auth contracts and tests. |
| `bot-indexer` | Disposable retrieval projection, bounded semantic chunks and rebuild/corruption behavior. | **Retain** Unicode/boundary/rebuild coverage. |

All four audit categories have dispositions: tests (F02/03/08 and package table), abstractions (F01/02/07/11), skills/instructions (F06/09/10), tooling/dependencies (F05/11/12). Detailed line-by-line review of every module remains outside the completed work.
