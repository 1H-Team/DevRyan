# Coding Agents continuation plan — 2026-09-05

## Compact checkpoint requested by the user

The September 6 [Fable 5.1 High follow-up review](second-opinion-review-r2.md) is complete, including corrections to its initial advice. The user will `/compact` and switch to Plan mode to review how implementation will finish. The UI/backend Plan correction is stopped at source `703b2c72…`: 132 backend and 169 UI focused tests, UI type-check, changed-file lint and root whitespace check pass. Further builds, full validation, live providers and performance execution are held. The two small performance-helper changes remain proposals only. The complete next-turn state is in [finish-plan-handoff.md](finish-plan-handoff.md); it supersedes older running/current-source wording below. The literal approved plan is retained in `.cache/audit/coding-agents-2026-09-05/approved-plan.md`: its required model/role/Plan/runtime cells, thinking states and natural-compaction coverage remain in scope. Exact 96/76 scenario expansion and repeated review overhead can be simplified only while preserving those observations or explicitly reporting blocked/unverified coverage.

## Latest scope

The user's 17:26 UTC clarification supersedes the original plan's wording about testing every configured model. Use one main model per provider:

| Provider | Frozen main model |
|---|---|
| OpenAI | `gpt-5.6-sol` |
| Anthropic | `claude-sonnet-4-6` |
| xAI | `grok-4.6` |

Additionally, investigate the user's reported **Codex 5.3-Spark Explorer** problem. Confirm the exact configured provider/model identifier and effective Explorer assignment before reproducing it. This is focused specialist-role coverage; it does not automatically add Spark to every Builder/Orchestrator combination.

The original outcome remains complete Coding Agents acceptance and evidence-supported fixes across Electron, web and responsive mobile web, including thinking semantics, real project work and compaction continuity. The user reiterated that **responsiveness, speed, performance and system load are first-class optimization goals**: measure and improve input latency, rendering, startup, CPU, memory and backend overhead where evidence identifies a bottleneck. Builder/Orchestrator, orthogonal Plan on/off, provider-default/explicit thinking and the original behavioral acceptance criteria remain in scope. Bots, VS Code runtime QA, releases, other repositories and personal configuration changes remain excluded.

## Completed and still open

- Latest result: `planref-r3` / `HaFl6X` is closed failed after eleven passing checks, two saved Plan revisions and one genuine native manual boundary. After the runner reloaded the UI, the next actual human steering submission had Plan off (expected on). All five screenshots are reviewed; the saved revision and fourteen original fixture files remain unchanged. Independent closure confirms 2,618 parseable journal records with no gaps and all 92 retained process identities absent. No second boundary, implementation or full continuity is established. Read-only diagnosis identifies generated managed-result turns as an incorrect cold Plan-restoration source and an associated backend continuation-policy gap; delayed-part hydration remains a separate regression hypothesis. These product changes are not yet implemented. The user requested a fresh Fable 5.1 High critical review of both the technical correction and the remaining acceptance process. Natural and broad execution remain held.
- Current operational run: root released the fresh `planref-r3` manual representative after the exact missing-import correction. Its runtime delta is one import line; two new actual-adapter regressions exercise initial/revised Plan capture and pass all twenty manual/natural tests. The manual regression reproduces the original ReferenceError before the fix. A targeted undefined-binding scan and independent review pass. Source remains `7b049793…`; scripts are now `bce9efea4b796007b6b1e596a11a6be368c700c8878f82328836f014ca5e7b80`. Scoped qualification is `manual-plan-import-correction-r1/delta-qualification.json` (SHA-256 `abea98778d89843f0eacd03d3847db845664b368a6b979af765e9303cc791c77`); the unchanged app retains its last full gate, with final full validation still required before complete acceptance. `Q2z8p4` is closed failed: all 67 process identities absent, fourteen original/archive files verified, both PNGs reviewed, and 1,711 gap-free journal records include 21 cleanup records beyond the copied manifest totals. The new run retains forty minutes and two native manual boundaries; natural and broad execution remain held.
- The `2aFUr9` Plan-mode failure is traced to a QA instruction conflict: the native Plan instruction prohibited project writes, while the helper required a separate project plan file. Both original saved app revisions and their full canonical text remain present. The QA correction pins the original app revision by session/source/human identity, path and bytes inside the prepared runtime's fixed plans directory. It requires an actual native read and a fresh full Plan card before approval; no copied or refed plan substitutes for that read. The related causal-grading correction requires an owned source read → native failed original test → owned source mutation → native successful original test for both roles, preserving actual managed child scopes. Seventy-four focused tests and both independent reviews pass. The combined full gate passes at 01:23 UTC on September 6, with no source/scripts drift (receipt SHA-256 `cd73d670e4cc58bbb000024dd56a147b7a8063c6e6f7b137db99d5a22f3cc59a`). After fresh independent package/config/process review, root released the new manual journey under `.cache/qa/compaction-representatives-mUsL9F-planref-r2/`; its launch log and `manual-activation-r1.json` retain admission pins. It ended failed after the first completed Plan turn: `findQaSubmittedUser is not defined` in the manual QA adapter. Both screenshots are reviewed, including the visible first Plan card; neither revision capture nor a native boundary completed. The exact missing import is being corrected with a regression that executes both initial/revision paths (it reproduces the ReferenceError before the fix). A separately identified manual config is prepared under `planref-r3`, held for that correction and cleanup review. The natural config remains unrun and separately held. Application/UI/native bytes are unchanged. Evidence and dependency assessment: `manual-plan-and-causal-corrections-r1/`, `manual-plan-reference-correction-r1/` and `manual-plan-reference-correction-r2/` in the audit cache.
- Current independent resource comparisons are complete on `mUsL9F`/`4TOqUt`: one-stream CPU +0.63% passes its regression limit; four-stream CPU +1.64% misses the 25% reduction target; both memory limits pass, with total app memory median −5.96% and separated ranges; GPU −1.62% misses the 50% target. All eighteen resource arms pass technical review. The original 36-arm ledger remains at eighteen passed, three failed and fifteen unrun; backend, typing and session-memory each stopped at their first failure. A separate fresh backend study now passes all six arms and independent review, without replacing or pooling original results. The resource applicability review finds baseline already contains the same animation/throttle implementations; those historical reduction targets do not establish a justified new optimization. Exact results and original failures are in [performance.md](performance.md).
- Current qualification includes delayed hydration, revised compaction coverage, Spark compatibility, foreground/navigation readiness, precise manual submission, typing-only measurement and the small image-extraction guard. UI/web builds, matching-backend native packages and the Electron core fixture are qualified in `.cache/audit/coding-agents-2026-09-05/post-profile-corrections/`. The later benchmark CPU-reader correction passes fresh complete validation with no source/scripts drift under `backend-cpu-normalization-r1/`; application/UI/native bytes are unchanged. Older complete gates and failed attempts remain historical evidence.
- An earlier packaged Electron candidate passed startup and all **148 interactive actions** in one independent functional run. This includes pagination, typing during background streaming, draft restoration, scrolling, disclosure controls, reconnect, cancellation and navigation. See `.cache/perf/2026-09-05T17-23-25-678Z-candidate-interactive-correctness-r1/summary.json` and the run's `closure-review.json`; this is not a current matched performance comparison.
- Closure review is complete: all four screenshots inspected, the complete 1,994,163-event trace parsed, 49 journal records with zero gaps/errors, and all 20 recorded processes absent. Cleanup required the recorded SIGKILL escalation. Closure receipt SHA-256: `21cac19e9067003e0ee5799250f1237ddd32c6dae9e90ae6e73fae02c6b3300e`.
- Full final live-provider acceptance and complete two-boundary operational compaction journeys remain open. Earlier partial retention evidence does not establish complete continuity.
- No persistent memory system has been added. Add one only if reproduced operational loss and matched controls justify it under the approved plan.
- The original R5 performance cohort stopped with two baseline pagination failures and one startup failure; its other 39 members remain unrun. The successful candidate functional run cannot replace a failed member or prove a performance improvement.
- The independent early resource block also stopped: baseline idle 1 and candidate idle 1 completed, then candidate idle 2 failed visible startup with a hidden document before the workload. Its remaining 15 observations are unrun; there is no accepted three-versus-three resource comparison. All recorded processes were confirmed absent. Evidence: `.cache/audit/coding-agents-2026-09-05/early-performance/resource-ledger.json`.
- The fresh current-graph backend AB/BA/AB study `.cache/perf/web-update-check/post-profile-normalized-r2-b62Dpm/` passes six arms and independent review. Median cold discovery is 3,433.05→3,014.56 ms (−12.19%); median per-arm maximum health latency is 3,386.29→3.00 ms, with separated ranges for both. All 53 recorded process identities are absent, six journal records have no gaps/errors, and all 3,387 original files are preserved. The original failed backend arm and five unrun arms remain unchanged. Comparison/execution receipts are in `backend-cpu-normalization-r1/`; the later study `independent-post-run-review.json` completes their initially pending review.
- The earlier fingerprinted backend cohort passed all six arms and 76 independent review checks. Median cold request: 10,151.37→8,052.16 ms (−20.68%); median per-arm maximum health latency: 10,137.94→6.40 ms. All 1,424 module fingerprints match except the intended module content; journals have zero gaps/errors, and all 111 recorded PIDs were absent during review. See `.cache/perf/web-update-check/early-cold-discovery-fingerprint-r2-20260905-XzgNkZ/independent-post-run-review.json`. This remains historical local fixture evidence, not pooled with the current study or evidence of renderer/whole-app acceptance. The prior colliding-label aggregate remains failed.
- The separate first-selection diagnostic `.cache/perf/first-select-only-diagnostic-r1/2026-09-06T00-25-59-873Z-first-select-only-diagnostic-r1/` delivers one click with exactly three trusted requested-row events and passes canonical selection. It intentionally stops before workload, so it neither explains the prior typing failure nor provides typing latency or a replacement arm. Independent cleanup confirms all sixteen recorded process identities absent (receipt SHA-256 `edd670a5079d89c8d31c7929b30847be4267f6dada6f0ba152b3fa7d43bcffdb`); the PNG review is complete.
- A rendered real-control regression confirmed that delayed historical message parts could overwrite a newer unsent effort choice. The corrected selection lifecycle passes eight mounted regressions and the full gate: manual intent survives hydration, failed sends, older queued snapshots and session changes; a matching successful send releases it for later canonical restoration.
- The retained historical journal had no exact Spark record, but the fresh `spark-explorer-pair-pcJve2` reproduced a concrete child API 400: Spark rejects `reasoning.summary`. Exact Explorer model and High effort were observed before adapter execution; no Spark output or tool activity occurred. The Sol control passed inventory, effort and managed-result checks, but its overall evaluator grade failed. A separate unchanged-fixture comparison reproduced Bun's large data-URL loader failure while Node passed, so the corrected probe used Node. Both original arms remain failed, both journals are gap-free, all 31 recorded process identities and both private runtimes are absent. The shared summary-policy correction passes 22 focused tests, the corrected live pair and the current complete gate.
- The early resource protocol explicitly omitted foreground setup. Its hidden document is not evidence of an acknowledged foreground request failing; that belongs to a separate older diagnostic. The new opt-in one-shot foreground mode is implemented and qualified for a separately declared cohort with unchanged admission deadlines and no retries. It does not establish the native cause or revive either stopped ledger.
- The corrected Node-launched Spark/Sol pair `.cache/qa/spark-explorer-pair-tjaZyz` passes all strict gates and independent review. Spark's five observed native requests retain High without summary; both exact inventories and managed dispositions pass. Journals contain 435/425 records with zero gaps/errors; all 44 process identities and private runtimes are absent. This verifies the compatibility correction in a focused HTTP workflow, not general model quality or UI acceptance.
- The opt-in foreground protocol passed its focused tests, peer review and the complete gate before the separate 24-arm cohort. Eighteen arms pass: complete idle, one-stream and four-stream scenarios with three fresh observations per side. Four of their five resource gates pass, but four-stream CPU falls only 1.14% with overlapping ranges, missing the 25% reduction target. The first plan-skeleton arm fails before measurement because injection reaches a null body after navigation; five remaining arms are unrun. The stopped ledger is preserved. Independent closure verifies 296 gap-free/error-free journal records and all 407 recorded process identities absent. Separate per-scenario aggregation is allowed by the original proposal; no six-gate acceptance is claimed. Evidence: `foreground-resource-r1/completed-scenario-comparison.json` and `cohort-closure-review.json` in the audit cache. The subsequent navigation-readiness correction passes 15 resource tests, syntax checks and peer/root review. It changes only the resource helper, its test and perf codemap; application source and packaged bytes remain identical. Delta qualification is in `plan-skeleton-document-correction-r1/receipt.json` (SHA-256 `9e06b229e668d5ac9748d6b3ca9b87bbb495b9c831a2a7cff524f705289006c2`). The new scripts hash is `5720b5f007a442730e44455ead033ce3dd5ff2f823bd22024a924398f39110c3`; foreground protocol is `36421246e3deeb5abf867126e356cd12ef01a4760ee3ee7cbe35a94ae93e90e0`. These are historical delta pins; new runs use the current qualification in Resume facts below.
- The first Spark/resource full gate failed two daily/weekly scheduler tests at persisted `lastStatus` (`running` instead of `success`); the other 354 server test files passed. The test-only correction replaces an arbitrary 200-read loop with a bounded wait for authoritative persisted success, preserving every dispatch, claim, final-state and restart assertion. Its exact 26-test module and the fresh second complete gate pass. The original failure and correction receipts remain preserved; production scheduler/runtime behavior is unchanged. The successful Spark pair remains applicable through the explicit test-only dependency assessment in `spark-and-resource-corrections/before-gate-r2.json`.

## Order after the user's `/compact`

1. **Get early whole-app and backend performance evidence on the frozen candidate.** Declare independent quiet measurement blocks using the existing backend update-check AB/BA/AB protocol and idle/streaming resource protocols, with three fresh baseline and three fresh candidate observations per scenario. Interleave resource arms prospectively to limit time/thermal drift; keep fixtures, workloads, sampling windows and thresholds unchanged. Verify each block's own artifact identity, source attribution and correctness. Baseline pagination is not a dependency of unrelated backend or resource measurements. Preserve stopped R5. A failure stops the affected block for classification, without automatic retry, substitution or expanded instrumentation. Continue independent blocks only when their own admission holds. Do not compare new candidate runs to selected historical failure prefixes. Any shorter interactive comparison must be defined prospectively and use fresh matched runs; CPU/memory results cannot replace responsiveness measurements. Use results to identify a concrete optimization, make the smallest justified change, and remeasure affected metrics.
2. **Carry the qualified focused corrections into operational acceptance.** Spark Explorer compatibility and managed-result reconciliation pass the corrected matched pair; delayed model/effort hydration is covered by the mounted regressions. The two compaction coverage corrections below are implemented and pass validation, while complete operational journey evidence remains open; the first manual representative has closed failed at its unobserved first native boundary. Use the current qualification/package pins and the documented Spark test-only dependency assessment. Preserve all earlier failed evidence and investigate any new runtime failure from its correlated journal and gaps before changing behavior.
3. **Complete representative operational journeys.** Use an available authorized provider to finish manual and natural compaction paths, each with two native boundaries and subsequent implementation, independent application/browser grading and managed-task reconciliation. Preserve user revisions, existing edits, current plans and unfinished obligations. Validate the revised natural witness policy through an actual journey; removing extra simultaneity does not prove feasibility. Do not launch the broad matrix until these paths work.
4. **Finish three-provider acceptance in resumable batches.** Reconcile the existing 96-scenario configuration against the three frozen models and required behaviors. Consider composing the high-effort core and project journeys, retaining separate grades and all required observations; merely appending a scenario ID currently launches another fresh run. Reuse prior evidence only with a documented dependency assessment covering served UI, backend, runtime and QA contracts. A server-only timing change may invalidate reconnect/cancel evidence even when UI bytes match. Keep failed, blocked and unrun results explicit. Fresh copied credentials must satisfy each run's admission window; an unavailable provider should not stall independent work.
5. **Complete final measurements and reporting.** Remeasure on the final candidate where intervening changes affect performance; distinguish early candidate results from final acceptance. Keep unavailable comparisons explicit and do not claim full acceptance while required evidence remains missing. Complete the concise audit, model/adapter findings, memory decision and reusable QA instructions.

## Corrections established by the second opinion

The [Fable review and coordinator decisions](second-opinion-review.md) distinguish accepted findings from unverified hypotheses.

- **Manual Electron coverage:** existing manual scenarios are web-only. Compose manual UI compaction into selected Electron project journeys at meaningful unfinished boundaries, with two native manual boundaries and separately graded continuation. Do not silently replace two-boundary acceptance with one smoke check. The original plan does not explicitly require 12 additional standalone manual Electron runs; choose coverage by required observations rather than duplicating a matrix mechanically.
- **Natural Orchestrator coverage:** the current harness requires an active child and a completed, undispositioned result simultaneously at boundary one, beyond the approved requirement to cover both states. Declare an explicit natural-only policy: active witness at boundary one; completed awaiting disposition at boundary two. Update readiness, native-start brackets, summary-exit observations, collection timing and final grading together. Preserve exact lineage and result identity, dispatch-attempt uniqueness across the entire journey, exactly-once disposition, two genuine automatic cycles, threshold evidence, gap failures and all Plan/implementation protections. Do not hold, recreate or infer a missing result witness. Leave the existing manual policy unchanged.
- **Spark profiles:** homogeneous `pinQaAgents` cannot reproduce a distinct Spark Explorer beneath the selected parent. Add only the necessary isolated QA assignment mechanism and its propagation checks; no personal configuration changes or automatic fourth broad matrix.

Prioritize product outcomes. Defer historical baseline startup investigations, observer refinements, additional package overlays and speculative infrastructure unless one concrete missing observation blocks an acceptance decision. Do not repeat the full validation gate on unchanged source; after code changes, run the appropriate focused/affected checks and the final required gate before acceptance.

## Resume facts

- Latest qualified package: `.cache/qa/packaged-electron-mUsL9F/package-evidence.json`; staged web UI: `.cache/qa/candidate-web-OxzBwy`. The matching-backend baseline is `.cache/qa/packaged-electron-4TOqUt/package-evidence.json`. Receipts are `post-profile-corrections/{candidate,baseline}-package.json` under the audit cache.
- Qualified source SHA-256: `7b049793696e14d2b2d682cbd094cce037b6f139274ab98cd13768ff57c65f1d`; current scripts: `bce9efea4b796007b6b1e596a11a6be368c700c8878f82328836f014ca5e7b80`; UI: `4b542bef1318fc30cf978994e8cd5bd4bda0448bec57d0d5aff7cb7e8f26ec90`. `manual-plan-import-correction-r1/after-scope.json` records the current manifests and the exact three-file delta since the last full gate; `post-profile-corrections/after-build.json` records the unchanged built UI. Completed resource arms retain their original `3a88…` scripts pin.
- Latest full validation: `.cache/audit/coding-agents-2026-09-05/manual-plan-and-causal-corrections-r1/full-validation-receipt.json`, SHA-256 `cd73d670e4cc58bbb000024dd56a147b7a8063c6e6f7b137db99d5a22f3cc59a`, passed at 01:23:28 UTC on September 6, exit 0 with no source/scripts drift. This qualifies the Plan-reference and causal-grading QA corrections; application/UI/native bytes did not change. The earlier benchmark gate and its `f57e…` scripts identity remain preserved for that study. The earlier Electron core fixture and four manual-request cases remain recorded under `post-profile-corrections/`: all 37 images reviewed, 1,618 journal records without gaps/parse errors, all 23 recorded process identities absent and the private runtime removed. That fixture proves request routing with expected 404s, not native compaction. Web bundle budgets pass; excluded VS Code remains over budget.
- Preserved earlier matrix: `.cache/qa/final-compaction-config-proposal-r1`, 96 singleton scenarios with 12 counted masters plus two excluded feasibility masters. Manual representative `qDE88v` ran on `Q1gHWd` from approximately 20:48 UTC and was stopped at 21:11 UTC after its first native boundary remained unobserved. Nine checks passed through revision-2 plan preservation and mixed task readiness. Missing click, submission lock and pending summarize transport remain unresolved alternatives. Its failed evidence is under `.cache/qa/acceptance-compaction-proposal-r1`, qualified by `spark-and-resource-corrections/manual-electron-project-r1-classification.json`; the other 95 scenarios are unrun. Original activation SHA-256 is `8d8ec5e890db53709e0447534174249fcbed1e6f885a26e79a1d8b739f0c5318`. Four original OpenAI Electron High project journeys compose two manual boundaries. Fresh current manual/natural configs use unique IDs/roots under `.cache/qa/compaction-representatives-mUsL9F-r1/`; all 110 prior configs remain unchanged. After diagnostic cleanup and fresh admission, root launched manual at 00:30 UTC on September 6. It ended failed at 00:41 UTC after both Plan revisions were saved and passed paused-file grading; the next check could not find `project/.opencode/plans/qa-current.md`. No native boundary or manual-submit request ran. Its output is `manual-evidence/mUsL9F-r1-openai-electron-orchestrator-plan-high-project-work-1/mUsL9F-r1-openai-electron-orchestrator-plan-high-project-work-1-2aFUr9` beneath that directory; `manual-activation-r1.json` and `manual-preflight-r2.json` retain admission evidence. The recorded native Plan instruction prohibited file changes, and both Plan-card texts explicitly deferred the requested project-file write until approval. This exposes a QA instruction-contract conflict; the normal persisted Plan revisions remain present. The Plan-mode reference correction and strict owned causal grading now pass focused/full checks and independent review. The separately released `planref-r2` manual run used the same current package, forty-minute budget and two-boundary requirement; it failed at the new exact-user lookup because the manual adapter omitted its import. The fresh `planref-r3` config is prepared only for the bounded import correction and executed-path regression. Natural and broad execution remain held. No stale witness or failed result is replaced.
- xAI copied access expired. The single normal-owner renewal attempt failed before login/provider work because regular DevRyan was unavailable. The user has been asked to reopen it; readiness remains unconfirmed. Do not relaunch the user's app or copy refresh credentials. Observe the new owner/port and perform fresh admission when xAI work is ready.
- The user continued at 18:39 UTC. The focused Spark pair remains verified within its documented dependency scope. A single sampled CPU diagnostic identified avoidable image masking; the narrow no-bracket guard passes focused/full checks and its six-process function benchmark. It is a small measured function improvement, not proof of the four-stream CPU target. Fresh affected resource and backend comparisons are now recorded; typing and session-memory comparisons remain unavailable.
- All historical resource ledgers remain stopped. The six blocks in `.cache/audit/coding-agents-2026-09-05/post-profile-measurement-proposal-r1/proposal-r2.json` have finished or stopped: one-stream, four-stream and plan-skeleton each complete three fresh observations per side; typing60, unchanged session-memory and the original current-graph backend each fail their first arm and leave five unrun. The ledger remains eighteen passed, three failed and fifteen unrun. The normalized-profile backend study is a separate six-arm success; no failed prefixes are pooled or relabelled. Source/script/artifact drift or unresolved owned processes holds later admission. Natural session-memory startup and full UI history loading remain unchanged.
- Native mechanical applicability is limited to the unchanged original chooser/menu/quit paths, recorded in `post-profile-corrections/native-Oi3VJf-to-mUsL9F-dependency-assessment.json`. The old backend graph includes both the changed image helper and changed fixture helper; `backend-current-applicability-mUsL9F.json` correctly required a fresh current-graph study, now supplied by `post-profile-normalized-r2-b62Dpm`. This does not broaden native or renderer acceptance.

See [macro review](macro-review.md), [Coding Agents acceptance](coding-agents-acceptance.md), [findings](findings.md), [performance](performance.md) and [QA instructions](../../QA.md) for the existing evidence and contracts.
