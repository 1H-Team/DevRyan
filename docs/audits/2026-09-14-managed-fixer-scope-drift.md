# Managed fixer assignment drift

## Evidence

The reported child was `ses_f6410a9e5fferCDNUE008Bu7jw`, titled
“Implement Claimed Package Membership Contract,” under the Booking Manager
Package Queue Dialog work. Its original user message
`msg_09bef5d2a00171Yi745ibnpEN9` assigned a bounded package-membership migration,
shared types/query keys, and queue hook. It explicitly excluded UI work,
bookings/slots/payment changes, remote migration deployment, and unrelated
verification repairs.

After timeout and authentication failures, a same-child model switch sent
`msg_09d4d347f001HPz1EgvtZ9GblP`: a generic continuation plus the model notice,
without the original brief. The child searched project history for its task,
acknowledged mixed older results, then announced “The assignment is Feedback
Chat Greeting and Form Reveal” in `msg_09d4e14c50016rgq5n20K8gvJi`.
That was a different task. Two native edit calls completed against a support
widget test file (`call-a1abdc06-0521-420a-8306-d1c6478a7370-56` and `-57`),
followed by unrelated test and browser verification. A later model continuation
preserved this mistaken task interpretation.

Context Mode returned shared project-index content labeled `current-session`.
The pinned source confirms that both default relevance formatting and timeline
ContentStore results use that label without proving calling-session ownership.
No replacement support-widget assignment appears in the recorded user messages;
the scope substitution appears in the recovered child's response after retrieval.

The journal manifest/gap check and diagnostics status reported no gaps. Tool
input fields and some paths are sanitized, so the evidence does not establish
the exact search arguments or full edit paths. The findings above rely on the
recorded original brief, continuation text, search results, assistant statements,
and completed tool events rather than reconstructed missing detail.

## Containment and correction

The exact running attempt `dvr_task_8468396672d5485eb54318807510f0f0` was cancelled
through the live managed-task API and returned `interrupted`. Other sessions and
the user's runtime were not stopped. The unrelated repository edits were not
reverted or modified by this investigation.

All same-child recovery and turn-budget prompts now attach the complete private
ledger assignment with identity, role, label, owned targets, constraints, and
acceptance checks. Missing identity/brief rejects dispatch. Exact recognition
supports the new envelope and older continuation records so restart recovery
retains its existing attempt bounds. The brief is reference context for remaining
work; completed commands are not replayed. Fixer and compatibility contracts
reject substituting retrieved history for the assignment.

Shared ContentStore results now say `project-index` in both search modes, with a
short statement that retrieved context cannot establish the current assignment.
Existing prior-session and auto-memory provenance remains intact.

Regression coverage includes provider recovery with misleading retained history,
lossless long briefs and trailing exclusions, malformed envelope rejection,
legacy/read-only recognition, provenance formatting and reversible hotfix
normalization. The disposable real-worker check additionally retrieves content
across synthetic sessions and checks both relevance and timeline labels.

These are source changes for the next built runtime and normal managed profile
provisioning. The running installed app was not patched or restarted. This fixes
the observed recovery/context failure; it is not a semantic sandbox for arbitrary
agent edits.
