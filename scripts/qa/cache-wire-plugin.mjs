// OpenCode invokes function exports as plugin factories. Keep this entrypoint
// default-only; the observer's testable helpers live in the adjacent module.
export { default } from './cache-wire-observer.mjs';
