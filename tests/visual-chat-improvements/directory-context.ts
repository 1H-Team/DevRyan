// No repository is selected in this provider-free component fixture. Keep the
// production Markdown renderer without starting the app's server sync owner.
export const useEffectiveDirectory = (): undefined => undefined;
